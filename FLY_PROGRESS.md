# Fly progress

Last updated: 2026-08-07 CEST

## Goal

Complete checkout across airline and travel-booking sites from semantic page state and the selected user's profile—not site-specific scripts or a blanket “skip extras” rule.

## Current focus — read this first

| Question | Current answer |
|---|---|
| Status | **EasyJet now traverses to the payment boundary. The current bottleneck is runtime latency and one incomplete Selected Booking price handoff—not simple dropdown capability or missing traveler data.** |
| Core component | **Incremental Single-Authority Runtime Conformance**, Roadmap **#3/#7/#13/#16**: one fresh observation, one TaskState goal, one governed action, one normalized verification result, and one authoritative state commit. |
| Root bottleneck | Trace `chk_msiw4zaiwm0q8c` completed the reversible journey but took about 4m43s while model work was only about 2.8s. Repeated full-state writes, duplicate field-result reporting, request acknowledgement, and large post-action DOM rebuilding dominated. The final `362 EUR` review was correct, but the earlier Selected Booking capture had admitted itinerary without total/currency. |
| Architectural correction | Keep complete private evidence and strict consequential boundaries, but make routine reversible interaction incremental. Local mechanical feedback may prove only a bounded popup transition; field, navigation, validation, money, legal, payment, purchase, and final completion still require canonical evidence. |
| Implemented universal fix | Observation persistence no longer rewrites session state before the loop; the loop owns the turn commit. Action result/status/event persistence is atomic. Governed field operations emit one result instead of two. Fresh retries no longer force a second full snapshot by default. Owned local popup transitions reuse the existing verification ledger. Selected Booking capture requires complete itinerary plus authoritative booking total/currency. |
| Safety correction found during validation | The mechanical fast path is structurally ineligible for price-sensitive outcomes and does not verify fields, stage navigation, validation, or terminal completion. It is an input to the existing verifier/ledger, not a second perception or completion authority. |
| Simplified / removed | Removed two redundant state commits from every normal planning turn, removed anonymous duplicate governed field reports, and removed unconditional full-snapshot escalation on freshness retry. No safety governor, exact target lease, semantic verifier, transaction invariant, payment boundary, or recovery budget was removed. |
| Performance | The code path is materially smaller, but the gain is not yet claimed live. The historical 3.7 GB SQLite database was preserved; a fresh restarted EasyJet trace must measure observation, request, persistence, report, and total wall time before promotion. |
| Preserve / regression boundary | Preserve profile logical-field authority, selected-booking baseline, exact action leases, stale-action refusal, policy-specific paid decisions, transaction reconciliation, semantic settlement, final terminal latch, and the no-payment/legal/card/purchase boundary. |
| Automated evidence | ✅ **297/297 unit tests**, **149/149 browser replays**, and `npm run check`. Exact regressions cover deferred observation state commit, bounded surface feedback, price-sensitive fallback, complete Selected Booking price admission, EasyJet quantity/skip-bags, travel purpose, payment boundary, and retained Kiwi/GoToGate/Turkish paths. |
| Done when | A restarted live EasyJet run reaches verified payment review materially faster with an approved itinerary/price baseline, no duplicate field reports or immediate duplicate page builds, and Kiwi or GoToGate remains green. |

### Immediate acceptance gate

1. ✅ Treat only structurally blocking modals/open choice surfaces as exclusive foreground; retain positioned itinerary/price summaries as page context.
2. ✅ Publish one bounded adaptive task when canonical compilation has no goal but exact low-consequence current controls exist.
3. ✅ Reuse the existing exact candidate, governor, executor, one-action diff, verifier, and failed-strategy memory—no parallel browser executor or site workflow.
4. ✅ Block paid, legal, payment, purchase, identity, itinerary, external-communication, and unowned unknown commercial CTAs before fallback selection.
5. ✅ Compact trace/event persistence without weakening the authoritative observation/governed-action ledgers.
6. ✅ Diagnose live trace `chk_mshja11iu8lys0`: surface ownership was correct; readiness was the remaining competing semantic veto before TaskState.
7. ✅ Make readiness hydration-only: reset deadlines per destination, pass stable pages with exact checkout-relevant controls to the controller, retain true blank/loading shells, and remove stable internal ambiguity → user handoff.
8. ✅ Add the complete live-shaped Continue → Seats unit replay and complete the full 143-case browser rerun.
9. ⏳ Reload extension/backend and live-test EasyJet followed by Kiwi and GoToGate canaries.
10. ✅ Live EasyJet `chk_mshk9hv0gp9ums` proved the repaired readiness/controller boundary by executing the safe seat path and reaching Cabin bags.
11. ✅ Cabin-bag scope, commerce price, free decline, and itinerary evidence now compile under separate authoritative roles; the exact live-shaped replay safely executes and verifies `Skip bags`.
12. ⏳ Reload the extension/backend and live-test EasyJet through Cabin bags, then retain Kiwi and GoToGate as canaries.
13. ❌ Live `chk_mshlu1mnfucogf` passed Cabin bags but exposed a separate Hold-luggage stepper failure: `Remove + price` fabricated selection despite exact quantity `0`; the agent exhausted two no-effect minus strategies instead of publishing `Skip bags`.
14. ✅ Quantitative state, Skip bags, travel purpose, remaining add-ons, and the payment boundary are now traversed live; the current failure class moved from semantics to latency/baseline acquisition.
15. ✅ Incremental single-authority conformance is replay-certified: one turn commit, atomic action report, one governed field result, bounded local surface feedback, and booking-total-required acquisition.
16. ⏳ Restart extension/backend, run EasyJet once, and accept only with measured latency improvement plus an approved `362 EUR` baseline; then run Kiwi or GoToGate as canary.

### Historical implementation log

1. ✅ **Seat episode live acceptance:** GoToGate advanced through both unselected legs and the final confirmation without a false paid-seat block.
2. ✅ **Unify terminal evidence:** typed, non-executable payment evidence now feeds browser classification, readiness, TaskState, and terminal ownership.
3. ✅ **Compile owned itinerary summary:** airport-code route pairs inside an exact travel-details owner become authoritative; arbitrary page-wide hyphens remain ineligible.
4. ✅ **Add exact replay:** the GoToGate-shaped card form, misleading confirmation sentence, travel summary, total, and terminal latch pass with no payment candidate; route-only shells remain transient.
5. ❌ **Live acceptance:** `chk_msbqdht2llspb8` reached the real GoToGate payment form and reconciled route/price facts, but terminal acquisition returned only `heading=true`; readiness expired after 15 observations/20 seconds.
6. ✅ **Universal repair:** visible owned payment labels and hosted widgets now emit additive evidence; empty native discovery is unknown; `paymentActionsAllowed=false` is preserved; exact positive and hidden-future negative replays pass.
7. ✅ **Live terminal acquisition:** `chk_msbs0ud3trktks` proved the additive contract and transaction reconciliation on GoToGate.
8. ✅ **Terminal dominance repair:** freshly completed terminal TaskState can no longer enter profile-failure arbitration; payment profile evaluation sees only exact pending contact controls; broad accessibility prose on an activation-only command cannot create a profile field.
9. ✅ **GoToGate live recertification:** `chk_msbvdlbsznwy56` returned `final_review` with terminal status and transaction review both verified; the false Pay/email field did not recur.
10. ✅ **Canonical instance and verified journal repair:** exact action lineage now reaches TaskState admission, same-label siblings are collision-free, parent/child confirmation commits before cosmetic surface exit, and repeated seat segments remain attached to one verified outcome.
11. ✅ **Coverage gate:** the transaction ledger is refreshed in the same turn when the journal changes, and payment review stays active with `verified_decision_outcomes` missing until all verified decisions are represented.
12. ✅ **Episode ownership repair:** a GoToGate stale bundle episode cannot block visible AirHelp/SMS sibling decisions; exact parent → child confirmation continuity remains intact.
13. ✅ **GoToGate live cycle acceptance:** `chk_mscf4cw69xjhdj` passed bundle, Flexible Ticket, both seat legs, seven later extras, review confirmation, and `/rf/payment` with no false cycle or irreversible action.
14. ✅ **Outcome-memory repair:** a verified exact commerce action now directly commits an idempotent typed outcome and expected ID; parent/child and repeated-seat episodes only enrich/merge that record. A no-episode AirHelp regression, the Flexible Ticket replay, and the two-leg seat replay pass.
15. ❌ **Live recertification regression:** `chk_mscz1j3loqi2sj` selected the correct free Flexible Ticket parent and child decline, then stale fallback episode ownership produced generic ambiguity and a paid-option attempt. The transaction governor stopped after the conflict; no irreversible action occurred.
16. ✅ **Universal lineage/effect repair:** explicit/fallback identity is separated; exact completed-parent and foreground-navigation ownership is restored; unapproved paid effects fail before dispatch; compact/bidi prices normalize; handoff cannot compete with a grounded safe action; journal/ledger coverage shares one canonical owner identity.
17. ✅ **Automated certification:** **260/260 unit tests**, **118/118 browser replays**, and `npm run check` pass, including retained-open Flexible Ticket, both seat legs, dirty sibling extras, review-modal submit/dismiss separation, and payment-review coverage.
18. 🟡 **Live runtime discrepancy:** `chk_msd0topc299a64` reached GoToGate `/rf/payment` safely in about two minutes but returned an empty journal/ledger. Replaying its saved observation/result through the current reducer produces the expected outcome, so the live backend must be restarted and re-certified.
19. ✅ **Compact owner repair:** a verified action now treats its resolved target snapshot as exact decision-owner evidence when compact transport omits the group from the action envelope. The live-shaped regression passes.
20. ⏳ **Next:** restart the backend, re-run GoToGate, inspect non-empty journal/ledger coverage at payment review, then re-run Kiwi as the regression canary.
21. 🟡 **Latest live result:** `chk_msd4hy91ah117b` reached `/rf/payment` in about 4.5 minutes with seven independent terminal signals and zero payment mutation, but the running process still returned an empty journal and incorrectly treated `0 expected / 0 recorded` as complete. The exact first bundle result journals under current source, confirming runtime-path mismatch rather than a new GoToGate perception failure.
22. ❌ **Intermittent combobox branch:** `chk_msd50mcbuttk4u` and `chk_msd523g1iyzgjt` entered the country-code query, observed a blocking dropdown with exact visible `Slovenia (+386)`, then stopped with `profile_control_binding_failed`. `chk_msd53k1y148ign` took GoToGate's direct-commit branch and continued, proving nondeterministic component settlement rather than bad profile data or user interaction.
23. ❌ **Fresh terminal recertification:** `chk_msd5ay5zlgg7ik` completed the checkout safely in about 4.5 minutes, but again falsely certified payment review with empty journal/ledger coverage. The exact first verified bundle result yields one journal entry under current source, proving the live runtime/source mismatch remains.
24. ❌ **Post-restart proof:** `chk_msd5r8w2cvtlc2` ran on the confirmed new backend, reached payment review, and safely stopped. Journal/ledger admission now executed, but only for one Flexible Ticket outcome despite nine verified commerce reports. Coverage falsely declared `1/1` complete because expected obligations are currently derived from admitted outcomes instead of the independent verified-result stream.
25. ✅ **Durable receipt-register repair:** verified exact commerce selections now enter an idempotent action-ID receipt register in the loop before TaskState admission. TaskState derives expected journal/ledger coverage from that register and backfills a receipt when a rerender erased its source decision. The new lost-bundle replay proves a receipt remains required after both page and transient action result are gone. **262/262 unit tests, 127/127 browser replays, and the full build/type/syntax check pass.**
26. ❌ **Fresh live contract failure:** `chk_msd6uhces8nzgj` reached GoToGate payment safely after nine browser-verified commerce choices, but persisted `0 obligations / 0 journal / 0 ledger`. The browser result was verified; authoritative transition evaluation then classified the overall turn as `progressed` because a successor decision remained and rewrote the result booleans to false. Offline receipt compilation from the original browser result succeeds. The next repair is to preserve local semantic verification independently from parent progress and feed that immutable local receipt into accounting.
27. ✅ **Local-success/parent-progress separation:** `transitionResult()` now preserves `localPostconditionSatisfied`, `localExpectedOutcomeObserved`, and `localOutcomeVerified` while recording overall `taskProgressStatus` separately. Receipt admission consumes the local contract when present; lifecycle planning may still remain `observed/progressed`. The live-shaped regression proves `progressed + exact free option verified` produces one receipt under its canonical owner. **262/262 unit tests, 127/127 browser replays, and the full repository check pass.**
28. ❌ **Fresh live partial acceptance:** `chk_msd7nfl36lj4co` safely traversed GoToGate to `/rf/payment`. Durable accounting improved from zero to `3 obligations / 4 journal rows`, and its expected set reported no missing admitted action IDs. It is not accepted: bundle, AirHelp, online check-in, mobile plan, SMS, and Platinum produced exact browser verification but were downgraded before receipt capture; Flexible Ticket also produced duplicate receipt/direct journal identities.
29. ❌ **Terminal authority split:** the payment page compiled seven terminal signals with `boundaryObserved=true` and `terminalEvidence.verified=true`, yet transaction provenance remained `unknown`, so `reviewFacts` stayed absent and the final action was safe `ask_user` with `payment_review` missing. The run stopped emitting trace events at 14:38:29 CEST; it is no longer progressing even if the sidebar still appears live.
30. ✅ **Single immutable evidence boundary:** raw exact-commerce receipt capture now runs before lifecycle evaluation, and reducer-side reconstruction from rewritten results is removed. Parent-task `progressed` status cannot erase the local obligation. Receipt/direct duplicates merge by action ID under the higher-authority receipt/episode identity; exact foreground child surfaces may inherit only their recorded parent episode.
31. ✅ **One terminal authority:** the browser fact compiler and backend normalization now consume `terminal-evidence/v1` to mark final-review facts. The former independent payment-page wording matcher is removed. A payment heading + owned review summary + at least one exact credential kind is represented inside the shared terminal compiler, while a lone card field or generic payment copy remains insufficient.
32. ✅ **Automated certification:** **265/265 unit tests**, **127/127 browser replays**, and `npm run check` pass. Exact regressions cover pre-lifecycle capture, rewritten parent progress, receipt/direct de-duplication, terminal evidence without provenance wording, progressive credential disclosure, GoToGate payment, Kiwi checkout, multi-surface Flexible Ticket, two-leg seats, stale actions, paid conflicts, and irreversible-action suppression.
33. 🟡 **Fresh live boundary proof:** `chk_msda69f4zpfr21` traversed GoToGate to `/rf/payment` in about 2.5 minutes. Eight browser exact-verification reports produced eight durable obligations and `missingActionIds=[]`; terminal review provenance now worked, leaving only `verified_decision_outcomes` missing. Safety remained green.
34. ❌ **Remaining canonical-identity failure:** same-page sibling receipts incorrectly inherited the previous episode in pairs: AirHelp/baggage, online check-in/mobile plan, and SMS/Platinum. The journal consequently retained 6 rows for 8 exact actions. Coverage also expected four transient JSON direct-owner identities alongside canonical receipt owners. The fix is narrower episode inheritance plus receipt-only expected coverage, not another site rule or lifecycle field.
35. ✅ **Exact episode-ownership repair:** a decision episode may own a receipt only when it records a concrete non-page child surface and the verified action's exact surface matches it. Ordinary page actions always retain the canonical owner compiled into their action contract. The regression proves a stale AirHelp page episode cannot own baggage while an exact Flexible Ticket confirmation child still aggregates into its parent.
36. ✅ **One expected-coverage authority:** expected action and decision IDs are rebuilt only from durable verified-action receipts. Previous coverage, direct journal admission, and journal rows no longer create expectations. Journal and ledger remain independent reconciliation targets, so a transient direct JSON owner cannot compete with a canonical receipt owner.
37. ✅ **Automated certification:** **266/266 unit tests**, **127/127 browser replays**, and the full build/type/syntax check pass. Kiwi checkout, GoToGate sibling extras, Flexible Ticket parent/child aggregation, two-leg seats, popup progression, terminal reconciliation, and irreversible-action suppression remain green. Fresh GoToGate then Kiwi live acceptance is next.
38. ✅ **GoToGate live acceptance:** `chk_msdar6yu7k7bor` completed contact/traveler data, bundle and Flexible Ticket declines, both unavailable-seat legs, six independent ancillary declines, review, and navigation to `/rf/payment` in about 3m06s. Final TaskState reported `8 obligations`, `8 expected action IDs`, `missingActionIds=[]`, `missingDecisionInstanceIds=[]`, `outcomeCoverage.complete=true`, `transactionReview.ready=true`, and `payment_review_reached`. One readiness wait after `Continue to Payment` was correct hydration handling; four stale observations were safely refused and recovered. No payment, card, legal, billing, or purchase action executed.
39. 🟡 **Non-blocking normalization debt:** the final journal had 9 rows and the transaction ledger 11 because Flexible Ticket's free parent selection and confirmation remain as additional compatible representations. They do not enter or pollute the receipt-only expected set, do not duplicate execution, and did not block terminal certification. Normalize them into one aggregate before treating ledger row count as a user-facing audit count; do not change the now-proven ownership or terminal gates while doing so.
40. ✅ **Fresh Kiwi canary on the repaired engine:** `chk_msdaz8oiuvr170` reached `payment_review_reached` in about 3m21s after traveler fields, no insurance, Basic Saver, no medical cancellation, skipped seat selection, later risk decline, and final contact fields. TaskState reported `5 obligations`, `5 expected action IDs`, `missingActionIds=[]`, `missingDecisionInstanceIds=[]`, `outcomeCoverage.complete=true`, and `transactionReview.ready=true`. One stale observation was safely refused; there were no destination waits and no payment, card, billing, legal, or purchase mutation.
41. 🎯 **Milestone transition:** Kiwi and GoToGate are accepted on the same universal accounting, readiness, policy, and terminal contracts. The next order-of-magnitude goal is Roadmap #17: validate a direct airline, then a structurally different OTA, turning each structural failure into a universal replay rather than adding a site workflow. Broader profile-policy scenarios follow structural coverage so perception/execution failures are not mixed with policy-matrix expansion.
42. ❌ **First direct-airline structural intake:** Turkish Airlines `chk_msdcgio30ui74p` and `chk_msdciegryzmzwm` stopped before mutation because the observer published `First / Middle name (as shown on ID)` as `field=middle_name`; preflight then truthfully but incorrectly asked for a missing middle name. The same observation classified a persistent 120px bottom itinerary/price/Continue bar as a blocking popover, suspending passenger/contact tasks. The immediately preceding easyJet `chk_msdchl2uxr5or4` classified its persistent right-side Price Breakdown panel the same way, proving the foreground defect is structural rather than airline-specific. Required repair: one composite given-names field contract, requiredness at the exact component/actuator level, and evidence-based blocking ownership. Do not add Turkish/easyJet selectors or invent a middle name.
43. ✅ **Universal composite-field/edge-chrome repair:** `given_names` is now a first-class logical profile fact across extension and backend, composed as first name plus optional middle name; a separate middle-name control remains independently enforceable. Persistent fixed/sticky bottom and side itinerary/price summaries no longer become foreground merely because they contain a button, while semantic dialogs, menus, dropdowns, warnings, and choice forms retain dominance. Exact Turkish/easyJet-shaped replays and the real seat-modal regression pass. Verification: **267/267 unit tests**, **128/128 browser replays**, and `npm run check`; fresh Turkish live proof remains.
44. ❌ **Turkish live promotion exposed split-node choice ownership:** `chk_msddkxyi4167uc` verified the prior repair and filled five canonical profile facts in about 15 seconds: given names, surname, DOB, email, and local phone. The bottom itinerary bar remained `surface=page`. It then published the visible `Mr./Ms.` widget as two missing generic `passenger:passenger` decision groups (`radio-owner` and nested `custom-choice-owner`) with no profile semantic or grounded candidate, producing `ask_user`. The visible country-code popup also lacked authoritative opener/surface ownership. Required repair: reconstruct one exclusive-choice owner, infer `title` from its exact option set, merge nested presentation/state/activation aliases, and bind portal options to the same logical component. No Turkish selector or policy exception.
45. ✅ **Universal split-node exclusive-choice repair:** canonical perception now resolves a nested presentation actuator to its proven outer exclusive-option state before identity or semantics. The complete owned option vocabulary types exact `Mr./Ms.` as `title`; wrapper and child share one control; `gender=male` compiles to one exact nested `Mr.` actuator; and verification observes `title=mr`. The live-shaped replay proves two options in one decision group with no generic passenger sibling. Existing native title/country-code, semantic-conflict, AirHelp option ownership, Kiwi/GoToGate checkout, paid-effect, modal, and payment boundaries remain preserved. Verification: **267/267 unit tests**, **129/129 browser replays**, and `npm run check`; Turkish live promotion is the remaining gate.
46. ❌ **Fresh Turkish live trace exposed option-value contamination after ownership succeeded:** `chk_msdrjx2f0js6qi` and `chk_msdrkoah17u4qu` executed the new compiler. Both options were `fieldType=title`, shared one `radio-owner`, and pointed to exact nested actuators (`atw-el-22` / `atw-el-24`). However, broad accessibility presentation text contained `Mr`, `Ms`, given names, and surname for each option; both `state.optionValue` values therefore became `mrs/ms`. The decision retained generic `passenger:passenger`, policy had no unique `Mr` candidate, and the safe handoff was unchanged. This was not stale runtime and not a failure of the wrapper/actuator merge.
47. ✅ **Exact option-value and semantic-owner completion:** canonical choice value now comes from the bounded owned option label at the option's exact ordinal, never from broad presentation text. Profile choices replace broad layout ownership in the decision contract, so the live-shaped contaminated-label replay emits distinct `mr` / `mrs/ms`, one `title:title` requirement, one exact `Mr` candidate for `gender=male`, and verified `title=mr`. Native title, country-code, ambiguity fail-closed, AirHelp, checkout, seat/modal, terminal, and safety replays remain green. Verification: **267/267 unit tests**, **129/129 browser replays**, and `npm run check`.
48. ❌ **Turkish live promotion exposed false requirement admission:** `chk_msds7a17nuke8h` selected and verified `Mr`, filled traveler/contact facts, and exposed an executable Continue. The backend nevertheless promoted optional Turkish-citizen, SMS/email marketing, and Miles&Smiles controls into unresolved decisions; page-wide repeated-label synthesis also merged `Select flight` with the unfinished phone-country selector. A legacy raw-button gate then overruled canonical Continue actionability and produced `ask_user`.
49. ✅ **Canonical Requirement Admission repair:** requirements now carry explicit `requiresResolution`; optional compatible decisions become `waived`, not missing. Unknown repeated CTAs require one exact local owner, profile-field openers stay out of commerce grouping, and the country prefix plus national number form one composite phone requirement. After profile-compatible `+386` settles, the canonical executable Continue is authoritative. No site selector or Turkish-specific policy branch was added.
50. ✅ **Automated certification:** **267/267 agent unit tests**, **130/130 browser replays**, and `npm run check` pass, including the exact Turkish-shaped optional-control/phone-prefix/Continue regression plus accepted Kiwi, GoToGate, modal, seat, terminal, accounting, and irreversible-action boundaries. Fresh Turkish live promotion and Kiwi/GoToGate canaries remain.
51. ❌ **Fresh Turkish live promotion exposed the next exact boundary:** `chk_msdu1xuxy4j6y2` filled given names, surname, DOB, email, and local phone; optional controls carried `requiresResolution=false`; and the backend planned, policy-approved, resolved, and dispatched the exact country-code opener. The browser verified `options_surface_appeared` and observed the visible country list. On the next turn, however, that portal surface had no durable parent/logical-field ownership, so no exact country option was admitted and Fly safely stopped with `phone_country_code.country_code=MISSING_EXECUTABLE_ACTUATOR`. This is Roadmap #5 cross-surface continuation, not a regression of #7 admission and not page lag.
52. 🟡 **Architectural correction after code audit:** portal ownership is the immediate evidence gap, but perfect deterministic ownership must not remain the only route forward for reversible mechanics. Existing `boundedRecovery` is still candidate-bound; `selectExecutableProfileGoal()` returns no goal when the popup option lacks field semantics; TaskState then nulls the goal; and the loop's profile-readiness branch stops before the existing surface candidate selector can run. Expand Roadmap #11 with one bounded adaptive surface goal carrying objective, desired canonical result, causally owned surface, allowed mechanical operations, forbidden effects, attempt budget, and semantic completion check. Reuse the existing current-surface candidates, structured model selection, governor, atomic executor, and fresh verifier.
53. ✅ **Roadmap and PRD alignment:** the North Star and transaction architecture are unchanged. Roadmap #11 is now explicitly the risk-tiered bounded adaptive surface operator rather than a candidate-only ambiguity selector; #9 distinguishes exact deterministic candidates from safe current-surface capabilities; the two pre-model actuator stops are the implementation seam; and Turkish full-loop `+386`, a second widget transfer, and Kiwi/GoToGate canaries are the promotion gate. Payment, background/iOS work, broad site expansion, and large refactors remain deferred until this gate passes.
54. ✅ **Bounded adaptive surface operator implemented:** TaskState retains a verified opener's exact profile objective inside only its current non-page surface; the candidate builder exposes grounded reversible choose/type/select/keyboard mechanics, ranks exact canonical matches first, and excludes paid/navigation/payment/legal effects; the existing selector receives the explicit envelope; the governor independently enforces surface, operation, risk, effect, step, and deadline limits; and verification rebinds changed presentation-derived control IDs through the stable state element. The Turkish-shaped replay now uses the real action pipeline instead of manual portal clicks and proves opener → portal → `Slovenia (+386)` → settled parent value → Continue. **269/269 unit tests, 130/130 browser replays, and `npm run check` pass.** Fresh Turkish live acceptance and second-widget transfer remain.
55. ❌ **Turkish live promotion exposed cross-lane exact-match starvation:** `chk_msekws5gmq313b` filled given names, surname, DOB, email, and local phone; opened the exact phone-prefix control; verified `options_surface_appeared`; and created the new `adaptive_surface` goal for `+386`. Slovenia was present as an exact goal-relevant candidate but at `y=8344`, `inViewport=false`, `executionLane=reveal`. Seven visible countries remained normal candidates, so the existing normal-before-recovery scheduler ignored the exact reveal target and called AI. The model was never reached because its packet was 25,950 bytes against the 24,000-byte hard limit. Required universal repair: rank exact goal compatibility before execution lane; suppress unrelated visible alternatives when an exact match exists; use the existing governed viewport recovery for the offscreen target; keep model fallback only for genuine ambiguity and keep its context bounded. No Turkish selector or country-list keyword rule.
56. ✅ **Exact-goal-first cross-lane scheduler implemented:** candidate construction now finds semantic exact matches across normal and reveal lanes before scheduling. An exact offscreen option is deterministic (`requiresJudgment=false`), incompatible visible alternatives are context-only, and the sole exact reveal candidate flows through the existing pending-action viewport recovery. Numeric matching compares bounded local numeric tokens rather than concatenating unrelated accessibility/identifier digits. Genuine adaptive AI packets compact foreground surface, goal, envelope, outcome, and labels before the 24 KB boundary; packet overflow now reports its real local cause. The browser replay proves visible wrong countries → exact offscreen Slovenia → reveal → fresh observation → exact `+386` commit → Continue. The stored live observation now reconstructs to zero generic normal candidates and exactly one Slovenia reveal candidate. **270/270 unit tests, 130/130 browser replays, and `npm run check` pass.** Fresh Turkish live acceptance remains.
57. ❌ **Turkish live promotion exposed duplicate action-surface authority:** `chk_msellaoxd0umyn` proved #56 live. It filled traveler/contact data, opened the exact phone prefix, verified the dropdown, retained the adaptive `+386` goal, suppressed visible wrong countries, and deterministically proposed Slovenia in the `reveal` lane. The proposal's current observation surface, adaptive envelope surface, and canonical `targetSnapshot.surfaceId` were all `atw-el-303`. `bindTargetSnapshot()` then normalized the action through the shared action contract, which does not retain a duplicate top-level `surfaceId`; `adaptiveEnvelopeFailure()` still required `action.surfaceId === atw-el-303` and falsely returned `ADAPTIVE_SURFACE_CHANGED`. Required universal repair: derive adaptive action ownership from the canonical target snapshot (or one shared surface accessor), compare it with envelope/current surface, and delete the redundant top-level prerequisite. Add the real normalize/bind/govern regression; the previous governor unit supplied `surfaceId` manually and the browser composition did not cross this exact governor seam.
58. ✅ **Canonical adaptive surface ownership implemented:** adaptive governance now reads action ownership only from the normalized bound target snapshot and requires exact envelope = fresh observation = target agreement. It no longer requires the transient candidate-only top-level surface copy. The live-shaped offscreen Slovenia action crosses candidate construction, target binding, shared normalization, and the actual adaptive governor; it passes the envelope and returns `TARGET_OUT_OF_VIEW` for the existing reveal lifecycle. A mismatched target surface remains rejected. Verification: **270/270 unit tests**, **130/130 browser replays**, and `npm run check`. Fresh Turkish live promotion is next; Kiwi/GoToGate must then be retained as live canaries.
59. 🟡 **Turkish direct-airline traversal reached payment but exposed the next evidence boundary:** `chk_msem2jloodwghg` filled five scalar traveler/contact components, opened the prefix, and selected exact Slovenia through the repaired governor. The old false surface stop did not recur. The dropdown contained a visible executable `editable_combobox` search control, but it was assigned to `surface-page` rather than the active dropdown, so the safe surface filter excluded it; Fly used two governed reveal scrolls and one reopen before selection. The site then returned an explicit “unable to process” modal after verified Continue, and Fly stopped safely; the user manually restarted. `chk_msem6y62iqkaax` resumed at seats, advanced additional services, and reached `/booking/payments/` autonomously. The final page visibly contained `FROM Sarajevo → TO Istanbul`, return itinerary, a payment-currency instruction, and `317.54 EUR`, while retained payment-method structures included Cash&Miles, voucher, Klarna, iDEAL, Apple Pay, UnionPay, and Alipay. Perception nevertheless classified `flight_selection`, compiled no itinerary, emitted only `method + review` terminal signals, and readiness expired without a terminal TaskState. The run is strong partial progress, not accepted end-to-end. Next universal repair: one positively owned progressive payment-entry contract plus bounded `FROM/TO` itinerary extraction; keep URL-only, payment-word-only, hidden-future-method, and placeholder-zero evidence insufficient. Secondary optimization: attach a search/filter input to its actual active surface and prefer a visible deterministic filter before an offscreen exact reveal.
60. ✅ **Progressive terminal + cost-aware mechanics implemented:** one shared terminal compiler now accepts visible progressive payment entry only when route/progress is paired with a strongly bounded `FROM/TO` IATA itinerary, travel timing, explicit payment/currency instruction, and exact currency total. It outranks generic `Select flight` chrome but never exposes payment actions; URL-only, hidden hosted markup, and generic payment copy remain negative. Transaction facts preserve both route segments, and a zero loading placeholder remains replaceable until complete authoritative identity exists. Portalled editable filters gain foreground ownership only through exact reverse ARIA linkage plus search/filter semantics; readonly dropdown openers remain page-owned. The deterministic order is visible exact, visible owned filter, offscreen exact reveal, then bounded ambiguity. **271/271 unit tests, 131/131 browser replays, and `npm run check` pass.** Next: fresh Turkish live acceptance, then Kiwi/GoToGate canaries.
61. 🟡 **Uninterrupted Turkish traversal live-proved the interaction repair and exposed price-role contamination:** `chk_msetj5a11ve2fi` completed traveler/contact entry, mechanically filtered with `386`, selected exact `Slovenia (+386)`, filled `70328922`, advanced the seat and insurance stages, and reached `/booking/payments/` in about 4m14s without manual recovery. The final page compiled both route segments, Ali SIFRAR, and `317.54 EUR`, but the collecting baseline had already frozen XCover's option-local `66.25 EUR` as `page_total` while itinerary identity was unknown. `UNAPPROVED_PRICE_CHANGE` was therefore a safe false positive, not a navigation failure or a real price change. No payment, card, legal, billing, or purchase action executed. Secondary non-blocking debt: exact title and country-prefix selections initially produced transient verifier misses, then appeared durably in the next fresh observation.
62. ✅ **Selected Booking Contract + price-role ownership implemented:** browser `transaction-facts/v2` publishes a whole-trip total only from a coherent selected-booking/payment owner and labels it `booking_total`; ancillary copy no longer enters `totalPrice`. HTTP compaction preserves role and owner. Backend normalization rejects typed totals without booking authority, baseline enrichment requires complete route/date/traveler/currency evidence, and both review and action-time price gates compare only approved like-for-like booking totals. The exact Turkish regression proves persistent `SJJ ↔ IST + 317.54 EUR` → XCover `66.25 EUR` excluded → payment `317.54 EUR` reconciled; `350 EUR` still blocks. A read-only sidebar diagnostic shows where Fly is, what it is doing, what is verified, its selected booking, and remaining transaction gaps. Verification: **273/273 unit tests**, **135/135 browser replays**, and `npm run check`.
63. ✅ **Selected Booking Contract live-certified on Turkish:** `chk_mseysb5cpjli9j` completed traveler/contact entry, exact Slovenia `+386`, local phone, seat/additional-service advancement, and reached the real payment page. TaskState approved the coherent two-segment baseline at `317.54 EUR`; the payment compiler independently observed the same itinerary/traveler/currency/total; review reported `ready=true`, `missingFacts=[]`, `contradictions=[]`; and the terminal latch recorded `payment_review_reached`. XCover `66.25 EUR` appeared repeatedly in raw evidence but never became `totalPrice` and never produced `UNAPPROVED_PRICE_CHANGE`. No payment, card, legal, billing, or purchase action executed. Remaining debt is operational rather than a failure of this fix: the country-code episode consumed about 3m38s and one rejected filter attempt before exact Slovenia settled; seat/additional-service transitions consumed about 1m17s. Outcome accounting was vacuous (`0/0`) because no explicit commerce-selection mutation was verified, so full Turkish site acceptance remains pending a minimal no-op-decision audit contract and retained Kiwi/GoToGate canaries.
64. ❌ **EasyJet exposed a reusable hidden-state/visible-actuator gap:** `chk_msez60hyieyxll` filled first and last name and compiled the exact profile goal `title=mr`. EasyJet's logical title control was an `editable_combobox` whose state actuator was zero-size and invisible, while two visible hit-tested trigger nodes were correctly located but remained `OPERATION_NOT_PROVEN`. The scheduler incorrectly treated the hidden node as revealable, performed three bounded scroll recoveries, then stopped with no blocker, mutation, or invented user question. Required repair: one local composite-widget graph that assigns hidden state and its unique visible trigger to the same logical control and proves `open → owned options → exact selection → durable value`; remove reveal recovery against non-rendered state when that actuator exists. Secondary evidence debt: route extraction admitted `NON-REFUNDABLE. CHANGEABLE SUBJECT → FEES.` and must be rejected by positive itinerary ownership. Latency analysis also shows the selected-booking compiler was not the main Turkish slowdown: backend round-trip time rose from about 110s to 202s because repeated country-widget turns transmitted about 19.2 MB total, including two 4.75 MB dropdown observations; final payment observation compilation cost about 7s.
65. ✅ **Turkish repeat canary preserved correctness but confirmed latency debt:** `chk_msfzb5l8oa9b0h` again reached transaction-verified payment review with approved SJJ→IST→SJJ, `317.54 EUR` baseline/payment agreement, `missingFacts=[]`, `contradictions=[]`, terminal latch complete, and no irreversible action. XCover `66.25 EUR` appeared 17 times in raw evidence and never became the booking total. The run took about 7m25s across 21 backend turns: roughly 226s backend round-trip, 28.9s browser observation, and 9.3s model/policy work. Total transport fell to 14.9 MB and the dropdown was uploaded once at 4.77 MB, but many ordinary 0.7–0.8 MB turns still consumed 8–34s; a seat full snapshot cost 16.1s and final payment observation 9.8s. Exact title and phone-country selections had transient verifier misses before durable state appeared, two stale Continue actions were correctly refused, and payment hydration waits were correct. Outcome accounting remains vacuous `0/0`. This is a successful safety/transaction canary and a failed latency target.
66. ✅ **Compact interaction/pre-surface discovery repair implemented:** model-facing candidate selection now consumes one `interaction-view/v1` projection rather than duplicate task, goal, and capability graphs. The runtime distinguishes `logicalControlId` from exact `actuatorId`, carries operation-scoped actuator geometry through binding/governance, and permits only one fresh, visible, hit-tested, profile-owned `open` hypothesis under `pre-surface-discovery/v1`. Navigation, paid, legal, consent, payment, stale, moved-actuator, and unverifiable operations remain denied. One action is followed by mandatory reobservation; no-effect strategies enter existing target-local failure memory; exact atomic choices still outrank discovery. Browser and backend route normalization now reject the live false fare-prose route. **274/274 unit tests, 135/135 browser replays, and the full build/TypeScript/syntax check pass. Fresh EasyJet live acceptance is next.**
67. ✅ **Composite settlement and exact short-enum matching implemented:** `chk_msg0xgv31ltvep` proved pre-surface discovery opened the EasyJet title widget and selected `Mr`; the remaining loop came from verifying only the blank visible shell while EasyJet retained `Mr` in a separate same-component state representation, followed by substring scoring that treated `Mrs` as compatible with `mr`. The logical-field adapter now merges only complementary exact-owner state/actuator representations, transports all exact state IDs and representation identity, and verifies the fresh canonical value from that bounded set. Adaptive exact option scoring uses equality/token boundaries, so `mr` cannot match `mrs`. The live-shaped replay proves one `Mr` click, zero `Mrs/Ms/Miss` clicks, `LOGICAL_COMPONENT_COMMITTED`, one resolved title component, and no repeated title goal. **275/275 unit tests, 136/136 browser replays, and the full build/TypeScript/syntax check pass.** Fresh EasyJet live promotion remains.
68. ❌ **EasyJet live-proved title settlement and exposed dormant-branch requirement contamination:** `chk_msg26ymqhrvb9z` filled first/last name, opened the title control, selected exact `Mr`, and returned `LOGICAL_COMPONENT_COMMITTED`. The next observation contained four email inputs at `0×0`: required full-page sign-in email, required signup email, hidden contact email, and hidden confirm email. The validation compiler unconditionally emitted `confirm email is empty`; requirement ownership accepted hidden HTML `required` or that synthetic validation even though no representation was rendered/actionable; the planner therefore stopped with three `MISSING_EXECUTABLE_ACTUATOR` entries. The same observation falsely marked the `Age at time of travel` placeholder as selected. Required universal repair: one active-lifecycle admission predicate, current-owner validation only, and one shared placeholder/commit rule. This is a new #4/#5/#7 boundary revealed after the #2/#11/#13 fix succeeded, not a regression of it.
69. ❌ **EasyJet live promoted the lifecycle repair and exposed missing pre-checkout booking authority:** `chk_msg6qfv1g3o9vc` filled names, recovered the title opener after two no-effect actuators, selected exact `Mr`, and verified the logical component. The next active requirement was correctly `age_at_departure`, but both page facts and the durable baseline contained `itinerary.segments=[]`; the passenger page exposed no usable departure date. Fly safely refused to calculate or guess and asked for age. The order-of-magnitude repair is to create the Selected Booking Contract when the flight is selected and persist it unchanged into checkout; mid-checkout fallback may promote only one coherent owned booking envelope. Ask for the missing departure date/booking confirmation—not the derived age—if acquisition is impossible. No EasyJet parser or hard-coded age/range.

### Core implementation steps — completed

1. 🟡 **P0 — Terminal dominance:** semantic payment-review evidence latches correctly, persisted latches suppress later turns, and fresh same-turn profile arbitration is now suppressed after completion. Automated and live recertification remain before promotion to proven.
2. ✅ **P1 — Contact-safe boundary:** known email/phone prerequisites may finish on a combined review/payment page, while payment, billing, legal, and generic candidates remain suppressed.
3. ✅ **P1 — Immutable logical identity:** mutable values and value-derived input meaning were removed from stable keys; controlled rerender replay preserves the same field identity.
4. ✅ **P1 — Strict transaction facts:** explicitly owned route endpoints survive; generic pairs such as `DIRECT → FLIGHT` are rejected by both perception and canonical normalization.
5. ✅ **P1 — Fact provenance:** route, fare, traveler, and total evidence carry typed owner/source/confidence records; unowned evidence is unavailable rather than guessed.
6. ✅ **P1 — Durable reconciliation:** approved identity remains stable while later verified facts fill prior unknowns; final review cannot rewrite the pre-review ledger.
7. ✅ **P1 — Process awareness:** TaskState exposes current position, verified achievements, unresolved obligations, current objective, and final verified outcome without becoming a second action authority.
8. ✅ **P2 — Regression proof:** clean, dirty, modal, hidden-CVV, contact, route, rerender, transport, blank destination, newsletter, live-shaped bundle-price, multi-surface decision-episode, composite-field commitment, real review-card, passenger-age route-noise, 245-seat foreground collection, two-leg terminal seat commitment, terminal payment evidence, and negative payment-shell replays pass; **252/252 unit tests** and all **124 browser replays** pass individually/currently.

## Latest live evidence

| Item | Status |
|---|---|
| Last verified win | Live trace `chk_msacv284ayq3q2` traversed traveler details, insurance, Saver fare, random seating, later protection choices, and contact entry to Kiwi's real payment review without the prior Kiwi site-error modal. |
| Enum-option proof | Nationality carried `{ canonicalValue: "si", siteValue: "si", label: "Slovenia" }` through the action contract and verified Slovenia. One first attempt was safely refused because the observation hash changed; the fresh retry selected Slovenia directly. No French Polynesia mutation occurred. |
| Remaining minor overhead | Kiwi hydration caused one nationality and several Continue actions to be refused before dispatch, but each recovered from a fresh observation within seconds. This is bounded freshness protection, not the next correctness bottleneck. |
| Transaction-integrity finding | The final observation now preserves traveler, Basic Saver, both included bags, no insurance, random seat, currency, and total. The baseline is approved and contradictions are empty. `review.ready=false` because route extraction preferred false phone/SMS text over the real itinerary. |
| Readiness result | This run proves the already-hydrated fast path remained correct after the readiness repair. It did not reproduce the earlier slow seat-loading shell, so live slow-path proof remains useful but is no longer the primary architectural bottleneck. |
| Current profile data | Traveler `trav_ccc7b6acac3d96ab` now uses `ali@aztela.com` for both contact and billing email and Slovenian phone `+386 70 328 922`. Historical trace rows below retain the earlier email because they describe what those completed runs actually entered. |
| Latest live result | Trace `chk_msacv284ayq3q2` reached Kiwi's real final review with Ali SIFRAR, Basic Saver, no travel insurance, random seat, included cabin and 15 kg checked bags, and 1,637.80 TRY. The saved email was entered and semantically verified. |
| Current stop | New trace `chk_msafk45ddm63rq` used the corrected profile and attempted local `70328922` with Slovenia `+386` already selected. The action reacted, but Kiwi replaced the input; alias rebinding failed and the fresh phone control was empty (`NORMALIZED_VALUE_NOT_VERIFIED`). Fly then stopped with `profile_control_binding_failed` because hidden optional billing first/last-name inputs were counted as active traveler controls. |
| Newest live result | Trace `chk_msago9keplhwfk` reached the real Overview & payment page in about 109 seconds. Ali SIFRAR, Slovenia, DOB, no insurance, Saver, random seat, AirHelp decline, `ali@testaztela.com`, `+386 70328922`, both bags, and 1,637.80 TRY were visibly correct. No Pay action or card number entry occurred. |
| Newest point of failure | Fly classified the final page as `confirmation`, never latched `payment_review_reached`, and continued after success: it selected payment-method UI and filled billing first/last name, address, and postal code before later stalling on generic ambiguity. This is a terminal-ownership failure, not a traversal failure. |
| Supporting identity defect | The phone control's stable key changed after fill because it incorporated `value:70328922` and value-derived meaning. The DOM value was correct, but verification could not rebind the pre-action logical field and falsely reported `NORMALIZED_VALUE_NOT_VERIFIED`. |
| Supporting transaction defect | Route extraction certified generic `DIRECT → FLIGHT` (and earlier generic pairs) instead of the visibly owned Antalya → Istanbul route. `review.ready=true` is therefore not yet trustworthy until endpoint ownership is strict. |
| Live safety acceptance | Trace `chk_msai899mz1wfey` reached Overview & payment and stopped in roughly 91 seconds, filled `ali@testaztela.com` and local phone `70328922` with `+386`, and executed no payment-method, billing, legal, card, or Pay action. The new terminal boundary therefore prevented the prior overrun. |
| Current certification failure | `paymentEvidence.boundaryObserved=true` and `pendingContact=false`, but the terminal latch remained active because `transactionReview.ready=false`. Missing facts were `itinerary_route` and `review_itinerary_route`; the false contradiction was `FARE_BRAND_CHANGED` from a polluted early baseline (`cancel your trip Upgrade your`) versus final `Basic Saver Edit`. |
| Current assessment | **Safe stop, not successful completion.** Fly behaved correctly by freezing rather than mutating payment/billing, but it should report `ready_for_payment` only after the owned review route and fare match the authorized transaction. |
| Local repair result | The exact unlabeled review-card shape now compiles `Antalya → Istanbul` with authoritative itinerary ownership and `Basic Saver` from the owned Ticket type row. Cancellation/upgrade marketing cannot become a fare, UI text such as Edit is removed, and final review cannot feed itself into the durable outcome ledger. |
| Latest live acceptance | Trace `chk_msajk04t0f70eb` completed traveler data, no insurance, Saver fare, no medical cancellation, random seating, AirHelp decline, and contact in about 99 seconds. Final review correctly read `Antalya → Istanbul`, `Basic Saver`, `1x Cabin bag`, `1x Checked bag 15kg`, `No travel insurance`, `Random seat`, traveler `Ali SIFRAR`, total `1,637.80 TRY`, email, and Slovenian phone. No payment action was planned or executed. |
| Latest terminal result | `processAwareness.currentPosition.stage=payment_review`, but `finalOutcome.achieved=false` and `transactionReview.ready=false` with the single missing fact `itinerary_route`. The durable baseline had no route; the final review route was correct but was intentionally forbidden from seeding its own baseline. Fly then asked the user instead of falsely claiming completion. |
| Transport-contract finding | The submitted observation contains canonical transaction facts, but `apps/web/server.js` sanitization preserves only route values and drops `transactionFacts.factEvidence` plus each segment's evidence. This is a concrete lossless-transport gap, not an airline-policy or browser-speed problem. |
| Local transaction-anchor repair | The exact live failure is now covered end to end: an untagged persistent checkout route becomes authoritative `bounded_checkout_route` evidence before final review; HTTP compaction retains owner/source/confidence/segment identity into an approved durable baseline; evidence-stripped typed facts fail closed. |
| Live Kiwi certification | Trace `chk_msap7ph8zcet8r` anchored `Antalya → Istanbul` before review, preserved typed evidence, matched Basic Saver, Ali SIFRAR, 1,637.80 TRY, both included bags, no insurance, random seating, and the declined later protection. After verified email and Slovenian phone entry, the backend emitted `payment_review_reached` and ordinary planning was suppressed. |
| Safety result | Zero payment-method, billing, legal, card, price-lock, feedback, Pay, or purchase action was planned. The terminal action was non-mutating `final_review`; the user remained in control at payment review. |
| Resolved evidence debt | The baseline route collection previously retained two passenger-age strings containing `2 → 12 YEARS`. Browser and backend contracts now reject those endpoints, merge duplicate route observations, preserve the richer dated segment, and retain exactly one authoritative `Antalya → Istanbul` fact in the live-shaped replay. |
| Review-proof result | The durable baseline is approved and commerce outcomes survived to review. `review.ready=false` only because route ownership chose two false phone-help segments and reported `review_itinerary_route` missing; no transaction contradiction was reported. |
| UI-noise finding | Feedback and price-summary controls no longer become alternatives inside checkout decisions merely because they share a broad ancestor or nearby “Included” text. |
| Safety boundary | An action is publishable only with a complete decision → option → exact actuator → semantic effect → profile compatibility contract. Unknown or contradictory price/effect evidence remains unavailable instead of becoming a guessed free action. |
| Newest GoToGate acceptance | Trace `chk_msbzh3c9doxhp1` reached `/rf/payment` in about 112 seconds and returned `final_review` / `payment_review_reached`. Seven terminal signals, Ali SIFRAR, both route legs, 93 EUR, and zero contradictions were present. No card, legal, billing, Pay, or purchase action executed. |
| Latest regression (2026-08-03) | Trace `chk_msccsn51psmw1t` correctly passed seats and selected the first post-seat `No thanks`, then stopped with `decision_episode_cycle_detected` / `No current semantic goal`. Six distinct active extras (AirHelp, baggage service, online check-in, mobile plan, SMS, Platinum) still had exact safe decline actuators. |
| Local owner-scope repair | A stale bundle episode is no longer carried into page-owned sibling actions; a detected cycle now excludes only that exact owner instead of erasing every goal. The live-shaped reducer regression and the two-leg seat replay pass. |
| Fresh owner-scope acceptance | Trace `chk_mscf4cw69xjhdj` reached `/rf/payment` and returned `payment_review_reached` after all GoToGate sibling extras. No `decision_episode_cycle_detected` stop occurred, so the exact-owner repair held live. |
| Latest correctness failure | Nine commerce declines were semantically verified in browser action reports, but terminal TaskState persisted `outcomeJournal=[]` and `transactionReview.outcomeLedger=[]`. `outcomeCoverage` also had zero expected IDs and therefore reported `complete=true`; terminal success is traversal-safe but decision-proof incomplete. |
| Local admission repair | Verified exact commerce results now compile directly from their action/postcondition contract into the journal and expected coverage set. A decision episode may only defer that direct record while a proven child confirmation is unresolved, or replace it with a completed richer aggregate. This removes the episode-only admission authority without weakening multi-step choice behavior. |
| Repair validation | **259/259 unit tests**, `npm run check`, and focused Playwright replays for Flexible Ticket parent/child and the two-leg seat episode pass. Fresh live proof remains required. |
| Latest performance note | The run took about 275 seconds from first action to terminal response. Eight actions were safely refused before dispatch with `OBSERVATION_HASH_MISMATCH`; 38 backend turns spent about 113 seconds in request transport. This is bounded safety overhead, not the primary correctness bottleneck. |
| Newest traversal behavior | One initially stale bundle action was safely retried. Flexible Ticket was the slowest localized interaction at roughly 30 seconds; the two-leg seat flow took roughly 23 seconds and did not stall. Six later ancillary declines completed without a loop. |
| Newest journal failure | The final persisted TaskState contains `decisionEpisode=null`, `outcomeJournal=[]`, and `transactionReview.outcomeLedger=[]`, even though multiple actions returned verified `EXACT_FREE_OPTION_VERIFIED` outcomes with nested episode IDs. Terminal traversal passed, but durable decision proof did not. |
| Exact identity evidence | All six later extras were assigned the same episode because a shared `contact:select-an-option` requirement outranked their distinct decision groups. Flexible Ticket's parent selector and confirmation also received different episode owners. Action lineage exists under `action.affordance.task` / expected postconditions, while episode continuation still reads narrower top-level fields. |
| Current assessment | **Accepted traversal and safety; failed outcome-memory acceptance.** The payment page proves destination and transaction summary, but Fly cannot yet prove exactly which reversible decisions it made on the way there. |
| Local journal repair | Decision lineage now travels in `affordance.task`; one normalized reader handles top-level and nested browser-result shapes; canonical identity contains hashed exact group and scope components under the transport limit; verified terminal outcomes upsert at `completed` or `completed_pending_surface_exit`; profile/navigation actions remain excluded. |
| Local coverage repair | TaskState records expected, journaled, ledgered, and missing decision instances. A journal mutation triggers one bounded same-turn invariant recompilation so terminal reconciliation never sees a one-turn-late ledger. Payment review remains active if any verified consequential decision is unaccounted for. |
| Regression result | **257/257 unit tests** and repository checks pass. The complete 127-test browser matrix passed before the final additive seat-segment assertion; both modified journal replays pass. A final overloaded matrix rerun passed 125 tests and timed out only in pre-existing hydration/oversized-transport cases, both of which passed immediately in isolated reruns. |
| Latest live run (2026-08-03) | `chk_msd0topc299a64` completed GoToGate traveler decisions, the free bundle, Flexible Ticket parent/child decline, both seat legs and final confirmation, six post-seat declines, review confirmation, and `Continue to Payment`; it arrived at `https://en-en.gotogate.com/rf/payment` in about two minutes. |
| Latest live safety | No card, payment-method, billing, legal, Pay, or purchase action executed. Three stale observations were refused and one bounded payment-page settling wait occurred; neither is the root failure. |
| Latest live bottleneck | Exact browser results such as `EXACT_FREE_OPTION_VERIFIED` retained `decisionGroupId`, `decisionInstanceId`, `decisionEpisodeId`, owner IDs, semantic effect, and postcondition through the next backend observation. Transport and verification therefore passed. TaskState still emitted zero journal and expected coverage records on every turn. |
| Replay correction | The current reducer replayed the saved bundle observation/result with `outcomeJournal.length=1`, a canonical expected owner, and the terminal episode outcome. The earlier child-surface explanation was not reproduced and is therefore not the root cause to patch. |
| Runtime conclusion | The empty journal from `chk_msd0topc299a64` is a process/version discrepancy: the saved live input cannot yield that state under the present reducer. Restart the backend before drawing another live conclusion. |
| Current validation | **266/266 agent unit tests**, **127/127 browser replays**, and `npm run check` pass. This includes receipt-only expectations, stale-page sibling isolation, exact foreground-child aggregation, compact-target ownership, and payment-review safety. |
| Latest GoToGate terminal trace | `chk_msbqdht2llspb8` completed contact/traveler data, bundle and Flexible Ticket declines, both no-seat legs and confirmation, later ancillary declines, final review, and navigation to `https://en-en.gotogate.com/rf/payment`. No card, legal, billing, Pay, or purchase action was planned or executed. |
| Latest transaction proof | The durable review baseline/current facts agree on outbound `AYT → SAW`, return `SAW → AYT`, traveler Ali SIFRAR, base `70 EUR`, total `107 EUR`, and no contradiction. Owned `Travel details` route extraction is therefore live-proven. |
| Latest terminal failure | The payment page was complete and stable (`documentReadyState=complete`, zero loading indicators, 2,031 characters of main content, 10 executable non-payment controls), yet terminal evidence contained only `heading=true`. Readiness returned `POST_NAVIGATION_DESTINATION_NOT_READY` through observation 15 and then `DESTINATION_CONTENT_MISSING_AT_READINESS_DEADLINE`. |
| Exact root cause | `observeTerminalStructure()` found no credential kinds on GoToGate's hosted/custom card surface. Because the structural object still declared an empty credential probe, `compileTerminalEvidence()` suppressed its visible-text fallback. Browser classification independently called the page `payment`, but the authoritative terminal contract remained false and TaskState could not latch completion. |
| Required repair | Preserve one terminal authority and make evidence channels additive rather than all-or-nothing: accept only visible, owned positive structure from payment field labels/wrappers/frames and active payment owners; keep all such nodes evidence-only; add a replay where card copy is visible but native credential inputs are opaque. |
| Local hosted-terminal repair | The exact opaque-widget replay now compiles `progress=true`, `form=true`, all four credential kinds, owned-label/hosted-widget provenance, `boundaryObserved=true`, and zero payment candidates. Its hidden future-payment twin remains `boundaryObserved=false` with `form=unknown`. |
| Terminal transport proof | `page.terminalEvidence` now survives HTTP compaction with signals, present/unknown states, credential kinds, evidence provenance, and `paymentActionsAllowed=false`; backend TaskState receives the exact browser contract instead of reconstructing it from sanitized text. |
| Current regression result | **252/252 unit tests**, **127/127 browser replays**, and `npm run check` pass. This includes accepted Kiwi checkout, blank destination readiness, both GoToGate seat legs, final no-seat confirmation, opaque hosted payment, hidden future payment, terminal HTTP durability, transaction facts, and irreversible-action safety. |
| Fresh GoToGate terminal proof | Trace `chk_msbs0ud3trktks` reached `https://en-en.gotogate.com/rf/payment`. The first route-only shell correctly waited; about nine seconds later the hydrated page produced seven terminal signals from payment route/progress/method/form/legal/review/heading evidence. The €448 transaction, Ali SIFRAR, and both `AYT → IST` / `IST → AYT` legs reconciled with no missing facts or contradictions. |
| Fresh terminal arbitration failure | TaskState set `goal.status=completed`, `terminalGoalLatch.locked=true`, `terminalStatus=payment_review_reached`, `transactionReview.ready=true`, and `paymentActionsAllowed=false`. The loop nevertheless ran profile failure handling first and replaced `final_review` with `profile_control_binding_failed`. |
| Exact false profile owner | Raw control `ctrl_button_h137xkqx` is role `button`, label `Pay`, and exposes only `activate`. Its broad accessible name contains nearby “e-mail confirmation” copy, so fallback logical-field inference labeled the button `email` and compared value `pay` with desired `ali@aztela.com`. This is invalid profile ownership, not missing user data. |
| Current priority | Make a freshly completed terminal TaskState the immediate authoritative return, then constrain profile logical fields to controls with field-compatible roles/capabilities. Preserve genuine pending-contact handling before completion and preserve the structural ban on payment/legal/card/Pay actions. |
| Avoid | No Kiwi selector, forced click, longer timeout, or removal of the safety governor. |
| First GoToGate acceptance | Traces `chk_msaq0o64jdfux1` and `chk_msaq57xy7w3q53` both filled `ali@aztela.com`, Slovenian phone `+386 70328922`, title, name, surname, and DOB correctly, then produced the same `NO_POLICY_ALLOWED_CANDIDATE` for the newsletter opt-out. The repeat proves a deterministic semantic blocker rather than a slow or incomplete page. |
| Current GoToGate blocker | The optional negated checkbox “I do not wish to receive any newsletters…” was compiled as a required `marketing_subscription` decision because its broad Contact owner contains other required fields. The profile is silent on marketing, so the safe planner handed off instead of guessing. |
| Latent GoToGate safety miss | Three visibly priced bundle choices (29/41/46 EUR) were compiled as zero-cost/included options and separated from the free “No, thanks” control. This did not cause the current stop, but it is the next high-risk ownership/effect error and belongs in the same universal decision compiler repair. |
| Non-blocking trace noise | Confirm-email and first-name were each filled twice after rerender/freshness checks. They verified successfully and did not cause the stop; optimize only after semantic correctness. |
| Newest Kiwi stop | Trace `chk_msav1t7zv4s50j` verified “No medical cancellation” and then a second free/no-extra action as `POLICY_SAFE_CHOICE_ADVANCED`. Kiwi changed to `activeStep=2`/Seating, but the first capture contained only progress chrome: zero decision groups, no seat option, and no Continue. Readiness reported `semanticReadiness=ready`, so the backend immediately emitted `ask_user` instead of reobserving. |
| Kiwi timing proof | The failing capture occurred roughly 0.4 seconds after dispatch. The prior accepted trace captured the equivalent successor after roughly 1.8 seconds and found Continue. This is a destination-readiness race, not a wrong profile decision or failed click. |
| Route-hardening assessment | The recent route diff does not call readiness or seat planning, so it did not directly create the Kiwi stop. However, the same trace compiled marketing prose (`— WITHOUT HAVING → SPEND EXTRA`) as an authoritative route, proving that blacklist-only endpoint filtering remains incomplete. Keep deduplication; replace broad separator-only acceptance with positive itinerary ownership. |
| Latest cross-airline result | Kiwi trace `chk_msaw7b789a79pn` retained the accepted checkout-to-payment-review behavior. GoToGate trace `chk_msawblzqv9kinr` correctly filled contact and traveler data and passed the former newsletter stop, then repeatedly selected the free Flexible Ticket outcome instead of advancing. |
| GoToGate loop proof | The trace selected “None of the passengers” 38 times and confirmed “I'll go without” 21 times. GoToGate visibly retained the free option as selected after each confirmation, proving execution worked; Fly's completion reconciliation failed. The existing repeat guard blocked only an unrelated confirm-email repeat because its identity is surface/action scoped rather than parent-outcome scoped. |
| Secondary compiler debt | The same live trace still compiled three explicitly discounted paid bundle choices as zero-cost/safe because written `Euro` text precedes the canonical `30EUR`-style token and nearby “Product included” copy wins. It did not cause this loop because Fly skipped the bundle, but the earlier replay is not sufficiently live-shaped and must be corrected before GoToGate certification. |
| Correctly skipped work | The optional newsletter requires no mutation when the profile is silent, and an unselected optional bundle requires no click merely to prove “no paid extras.” Fly should act only when a required/profile-relevant decision remains unresolved. |
| Local episode repair | The exact dropdown → free option → child confirmation → selected parent → close sequence now executes each semantic step once. The selected option cannot be republished, and only the unique expanded choice opener may close the completed foreground dropdown. |
| Local bundle repair | Accessibility-owned `aria-describedby` price evidence and compact tokens such as `30EUR` now override nearby “Product included” benefit copy. The replay compiles 30/42/45 EUR as paid and joins the exact free decline into the same optional decision. |
| Regression result | **249/249 unit tests** and **122/122 full browser replays** pass after deferred-choice commitment. Existing Kiwi checkout-to-payment-review, seat, direct/native choice, completed-dropdown, and payment-safety replays remain green. |
| Latest GoToGate progress | Trace `chk_msayckcjf2yoni` proves the prior semantic loop is gone: “None of the passengers” was selected once, the child “I’ll go without” confirmation verified as `POLICY_SAFE_CHOICE_ADVANCED`, and the selected parent outcome was not republished. |
| Current GoToGate stop | The completed dropdown remained open. Fly planned the correct opener action (`ctrl_button_h15w3am3`) but the governor returned `CAPABILITY_EXECUTION_LANE_DENIED` before browser dispatch. Continue was visible behind the foreground and correctly remained suspended. |
| Current root cause | Candidate building cloned the page-owned opener onto foreground surface `atw-el-217`; governance classified against the original page-owned control and rejected the surface binding. This is capability-proof discontinuity between layers, not browser slowness, a wrong profile decision, or failed option selection. |
| Latest GoToGate traversal | Trace `chk_msbnzmvhdlkm2t` passed the repaired contact field, bundle decline, Flexible Ticket decline and parent exit, bounded both seat maps, and advanced through flight 1 and flight 2 to the final “Are you sure?” no-seat confirmation. This is substantial live progress and proves the prior perception, logical-component, loop, and foreground-performance repairs remained intact. |
| Exact final action proof | Fly grounded the popup's exact `Continue` actuator (`seatMap-allSeatsSelectedConfirmation-dismiss`); it was current, visible, enabled, hit-tested, foreground-owned, policy-allowed, and bound to `active_surface_dismissed`. The failure was not button discovery or execution. |
| Current live blocker | In the same final observation, the canonical transaction layer introduced `seatmap_true / Add to cart` as `seat:seat_assignment`, `committed_paid`, even though both legs remained unselected and the foreground explicitly confirmed that no seat had been selected. The governor returned `UNAPPROVED_SELECTED_EXTRA` and correctly prevented dispatch. |
| Decision-episode finding | The current episode contract is not durably scoped to the seat decision: commitment is inferred again from surface wording, and episode history can span unrelated prior decisions. The earlier `Not selected` replay covered one child surface but not the semantic transition to “you haven't selected a seat.” |
| Safety result | No paid seat, payment, legal, billing, card, or purchase action executed. The stop is functionally wrong but safety-correct; weakening the governor would hide the upstream truth defect. |
| Latest GoToGate acceptance | Trace `chk_msbp299gi8ivym` passed the bundle, Flexible Ticket, both seat legs, final no-seat confirmation, seven later ancillary declines, traveler review, and `Continue to Payment`. It reached the real `/rf/payment` page with Ali SIFRAR, `ali@aztela.com`, `+386 70328922`, outbound/return itinerary, included hand and checked bags, and 448 EUR. The prior seat blocker is fixed live. |
| Current terminal stop | The payment page was stable and fully rendered, but readiness classified it as `POST_NAVIGATION_DESTINATION_NOT_READY` for 24 observations and then `DESTINATION_CONTENT_MISSING_AT_READINESS_DEADLINE`. `strongPaymentEvidence=false`; Fly handed off instead of emitting `payment_review_reached`. |
| Exact classification defect | Browser classification matched generic “Booking confirmation and updates…” before stronger payment-form evidence and labeled the page `confirmation`. Backend readiness saw the `/rf/payment` route but not card fields because payment credentials are intentionally absent from executable canonical controls, leaving only one of the required payment signals. |
| Transaction-certification debt | Persisted review remained `ready=false`, missing `itinerary_route` and `review_itinerary_route`. The visible route was not compiled, the review fare was blank, the baseline fare was incorrectly `flexible`, and the outcome ledger omitted the terminal seat result and several later declines. This is the next truth-reconciliation layer after terminal detection, not permission to continue into payment. |
| Latest safety result | No card, legal checkbox, billing, Pay, or purchase action executed. The terminal stop was safety-correct but semantically false because usable payment-review evidence was already present. |
| Terminal evidence implementation | `terminal-evidence/v1` is now emitted by browser perception and consumed by stage classification, readiness, TaskState, and the terminal boundary. It is explicitly evidence-only with `paymentActionsAllowed=false`; payment fields and Pay remain absent from executable candidates. |
| Exact replay result | The live-shaped GoToGate page containing incidental “Booking confirmation and updates…”, active Payment progress, Card number/Expiry/CVV/Cardholder, payment method, legal checkbox, Pay, outbound/return travel details, bags, and 448 EUR now classifies as payment and latches with a verified transaction fixture. A `/rf/payment` route plus generic confirmation copy and no form remains transient. |
| Transaction owner extension | `Travel details` now qualifies as an exact itinerary owner. Airport-code pairs rendered as `AYT Antalya - SAW Istanbul` and the reverse compile only inside that owner; broad page hyphen parsing was not enabled. |
| Current verification | `npm run check` passes; **252/252 unit tests** pass. The full browser batch passed 123/124; only the pre-existing 245-seat performance replay exceeded its 30-second budget under batch load, then passed focused in 19.7 seconds together with the payment and checkpoint replays. |
| Next live gate | Run GoToGate from a new checkout. Success requires immediate payment readiness, an approved/reconciled transaction review, `payment_review_reached`, and zero card/legal/billing/Pay actions. Then rerun Kiwi as the accepted canary. |
| Latest live phone failure | GoToGate traces `chk_msazljq8l7h19l` and `chk_msbl6gsth3abbv` both typed the saved `+386`, opened a one-option `Slovenia (+386)` foreground listbox, and then stopped with `MISSING_EXECUTABLE_ACTUATOR`. The stop observation itself contained a proven, visible, enabled, hit-tested `choose` actuator for that exact option. |
| Comparison proof | Earlier trace `chk_msayckcjf2yoni` issued the same country-code action, but the widget committed `+386` without leaving the listbox open, so Fly immediately filled `70328922`. The divergent browser outcomes expose a missing multi-step field episode; they do not show incorrect profile data. |
| Revert assessment | Do **not** revert the completed-choice surface repair. Its executable exception is gated to `semanticType=completed_choice_surface`; the failing country-code path is produced by the older profile-field selector, which has no child-surface continuation. Reverting would restore the prior Flexible Ticket governor failure and would not supply the missing phone-option handoff. |
| Supporting validation debt | Confirm email was visibly and canonically `ali@aztela.com`, with `valuePresent=true` and `invalid=false`, but a stale owner error still said the field was empty and produced `NORMALIZED_VALUE_NOT_VERIFIED`. This did not cause the terminal stop, but the field-episode verifier must retire stale validation evidence when the fresh exact owner is valid. |
| Local component-commit result | The exact live-shaped country-code branch now remains pending when typing `+386` opens suggestions, selects only `Slovenia (+386)`, verifies dropdown settlement plus canonical parent value, and then advances to `70328922`. A direct commit still completes atomically. Fresh valid owner state retires only stale presence errors; real format/mismatch errors remain blocking. |
| Latest GoToGate live result | Trace `chk_msbm3wcj7238iv` passed the country-code child surface, traveler data, bundle decline, Flexible Ticket decline/confirmation, completed-dropdown exit, and reached Seat selection. The earlier logical-component blocker is therefore fixed live. |
| Seat timing proof | Continue dispatched at `09:44:32.937Z`; UI settle completed in 497 ms and GoToGate exposed a 245-option seat popover. `verify_click` then spent **97,578 ms** in one `material_rescan`; semantic verification itself took only 67 ms and proved `CHECKOUT_STAGE_ADVANCED`. |
| Current stop | After the verified stage transition, no `action.report`, `action.lifecycle.finalized`, or next loop event was persisted. The current implementation lets the foreground seat collection escape the page-level high-cardinality bound and then duplicates it through surface/visual/diagnostic objects. This is perception/transport backpressure, not waiting for Kiwi/GoToGate or uncertainty about which choice the profile allows. |

## Core system status

| Component | Status | Note |
|---|---|---|
| Fresh transactional observation (Roadmap #3, feeding #13) | Implemented and live verified | Material DOM mutations carry a monotonic version. Planning accepts only an atomic snapshot whose version remains unchanged; a control appearing during/just after a build invalidates that capture and triggers one bounded fresh rebuild. Clean cached observations are reused immediately. |
| Semantic checkout compiler | Built and hardened | Owns the canonical decision, option, exact actuator, semantic effect, and option-local price contract; absolute fare totals normalize only when one baseline is uniquely proven. |
| Profile decision resolver | Built and simplified | Consumes compiled effects; it no longer treats `Continue`, an unknown sibling, or an arbitrary selected option as proof of a free/profile-compatible outcome. |
| Logical component commitment (Roadmap #5 → #9/#12/#13) | Implemented and replay-verified; live GoToGate acceptance next | Scalar values retain their simple contract. Choice-capable values remain pending across parent input, child option surface, exact selection, popup settlement, and canonical verification; direct/native and Kiwi paths remain green. |
| Transaction facts and review gate (Roadmap #15 using #1/#3/#6/#13) | **Implemented, replay-verified, and live-proven on Kiwi** | The boundary prevents all post-contact payment/billing actions. Owned itinerary/fare evidence, gradual baseline completion, non-self-seeding review, exact canonical reconciliation, and terminal completion passed live. Cross-airline validation is next. |
| Readiness boundary | Implemented and replay-verified; awaiting live slow-path proof | Readiness consumes durable action/lifecycle evidence, so a destination shell cannot become a generic goal merely because observation-local feedback is absent. Exact stage controls or the shared deadline close the commitment. |
| Transition verification | Fixed and tested | Supports exact same-surface commitment and profile-authorized safe stage transition without weakening price, validation, or paid-selection guards. |
| Active decision lifecycle | Built and tested | Shared availability is `active`, genuinely `revealable`, or `inactive`; clipped retained controls cannot recreate completed decisions, and retirement no longer requires physical DOM deletion. |
| Bounded observation/perception | Implemented and live verified | Structural repeated-action discovery now runs before canonical compilation and does not depend on each item's label or price. The latest live Seating flow no longer produced the prior 35–70 second builds and resolved its free exit in seconds; explicit-seat replay coverage still retains the full collection. |
| Multi-surface decision episodes (Roadmap #6/#7/#9/#13/#14/#15) | Implemented and fully replay-verified; live GoToGate proof next | Episodes rotate by semantic family/subject, preserve repeated segment results, and commit only after verified terminal confirmation. Transaction facts consume that terminal result instead of reinterpreting background presentation state. |

## Progress log

| Time | Done / learned | Result |
|---|---|---|
| 2026-07-31 13:06 CEST | Ran Kiwi flow through traveler details, package choice, and seat page. | Correct fare selected; seat confirmation modal reached. |
| 2026-07-31 13:20 CEST | Traced the stop from client logs and readiness evidence. | Root cause isolated to readiness precedence, not profile policy or click execution. |
| 2026-07-31 13:25 CEST | Reduced the proposed repair to foreground ownership, scoped stage inference, and one exact replay. | No site-specific handler or timeout change is needed; duplicated foreground membership logic should reuse the shared surface contract. |
| 2026-07-31 13:38 CEST | Implemented scoped readiness, foreground choice reconstruction, and profile-resolved seat gates. | Exact Kiwi trace becomes `READY`, plans only Skip for the saved profile; 214 unit tests, 94 browser replays, and build/type/syntax checks pass. |
| 2026-07-31 13:49 CEST | Traced the next Kiwi stop through click, verification, persistence, and loop scheduling. | Kiwi advanced `extras -> seats`, but the source choice disappeared and failed same-surface verification; `/agent/report` never acknowledged it, the governed action stayed `approved`, and no `loop.schedule_next` event followed. |
| 2026-07-31 14:04 CEST | Implemented transition-aware choice completion, compact bounded result persistence, and shared action finalization. | New fast-navigation replay passes; all 215 unit tests, all 95 browser replays, and build/type/syntax checks pass. |
| 2026-07-31 14:14 CEST | Traced the new Kiwi stop at medical cancellation through the authoritative goal and candidate set. | Not a hang: the free decline was compiled and policy-allowed but excluded as goal-irrelevant; a paid lock-price card became the navigation goal through broad label matching. Minimal repair is goal scheduling/typed navigation, not a Kiwi handler or timeout. |
| 2026-07-31 14:24 CEST | Checked the first-page insurance behavior in the same trace. | The agent skipped the unresolved No insurance choice and attempted Continue three times; no No insurance action was recorded in this run. This is the same scheduling defect: navigation is prioritized before a profile-resolvable stage gate. |
| 2026-07-31 14:29 CEST | Implemented typed safe navigation, profile-resolved exclusive stage gates, medical-cancellation semantics, and policy-scoped no-candidate handoff. | Trace-derived insurance and lock-price regressions pass; all 218 unit tests, all 95 browser replays, and build/type/syntax checks pass. |
| 2026-07-31 15:44 CEST | Dissected live trace `chk_ms8znsmehhxsek` through Seating. | The previous gate fix worked, but the final seat observation had 493 controls and exceeded 5.5 MB after compact retry. Secondary defects: a broad trip-summary container was misclassified as a free decline and global “Open sidebar” was proposed; stale-observation verification prevented the latter click. |
| 2026-07-31 16:08 CEST | Implemented bounded repeated-control collections, profile-aware seat retention, exact checkout relevance, price-summary ownership guards, and a four-second identical-navigation settling guard. | The Kiwi-sized 490-seat replay shrinks below 1 MB while preserving Continue and collection evidence. All 219 unit tests, all 98 browser replays, and build/type/syntax checks pass. |
| 2026-07-31 16:17 CEST | Dissected live trace `chk_ms90tb9lq6mwku` through DOB, insurance, Seating, and AirHelp. | Seating is fixed live. DOB was cleared by Kiwi's insurance rerender and correctly refilled. The final stop is a contradictory semantic contract: AirHelp is owned by passenger, the free option is bound to Learn more and marked paid, and the governor therefore has no safe candidate. Later comparison confirmed this run also clicked Feedback through the same false trip-summary decision before recovering. |
| 2026-07-31 16:24 CEST | Reassessed the failure against `FLY_FINAL_ROADMAP.md`. | The architectural gap is the boundary between stable logical identity, the requirement/decision engine, and the grounded candidate builder: perception finds the elements, but option-level ownership does not yet enforce one coherent label/state/price/effect/actuator contract. DOB is a separate semantic-postcondition durability gap. |
| 2026-07-31 16:43 CEST | Implemented the option-level contract across perception, decision reconstruction, candidate publication, and postcondition verification. | Local decision ownership now outranks broad sections; native choices use only exact radio/label/proven proxy actuators; signed option prices outrank benefit amounts; mutable prices cannot change decision identity; expanded choices reconcile with their collapsed selected field; and utility chrome is excluded despite accidental geometry. Trace-shaped AirHelp/Feedback coverage added. All 220 unit tests, all 99 browser replays, and build/type/syntax checks pass. |
| 2026-07-31 16:53 CEST | Compared live trace `chk_ms921oim7x2r1d` with the earlier `chk_ms90tb9lq6mwku` run and followed the chosen control through perception, policy, and execution. | The earlier run was not correct at this point: the same fake free summary option actuated Feedback and happened to recover. Excluding Feedback exposed Lock price as the next wrong actuator. Root cause is broad ancestor-based custom-button ownership plus free inference from unrelated “Included” text, amplified by policy-side `exactPolicyResolvedFree`. Existing summary replay checks View price breakdown but not Lock price, so it missed the unsafe action. |
| 2026-07-31 17:07 CEST | Implemented atomic custom-decision ownership, exact custom-actuator price precedence, and removed policy-only free-option manufacture. Added live-shaped Lock price and AirHelp regressions. | A broad ancestor cannot absorb a nested coherent decision or unrelated summary control; a paid exact actuator cannot inherit free status from parent copy; policy can filter proven free controls but cannot create them. All 221 unit tests, all 99 browser replays, and repository build/type/syntax checks pass. |
| 2026-07-31 17:31 CEST | Dissected live trace `chk_ms93gz5u9ma4dv` and compared its fare contract with the previously successful TRY traces. | This is a real system gap, not a slow page or stale browser. Perception found Saver/Standard/Flexi and all exact actuators, but Kiwi now displayed absolute totals (33/45/58 EUR) instead of `Included` plus signed deltas. The compiler left every option unpriced; policy treated all three as eligible, and the stricter candidate boundary correctly refused to invent a free choice. Previous behavior only appeared to work because the older page exposed explicit `Included`/`+delta` evidence. |
| 2026-07-31 17:34 CEST | Followed the live fare from semantic compilation through normalized policy, TaskState, grounded candidates, and selected-state completion. | Do not revert atomic ownership or restore policy-manufactured free candidates. Remove the broad shortcuts instead: `continue => included`, unknown sibling `=> included`, `constraint => selected_free_option`, and `any selected + constraint => satisfied`. Add owned absolute-total to incremental-delta normalization and require the selected/allowed control to be the uniquely proven policy match. |
| 2026-07-31 17:55 CEST | Completed the canonical Decision-Effect Compiler path and removed the duplicate downstream guesses. Added compiler and browser regressions for Kiwi's absolute fare totals and conflicting selected state. | `33/45/58 EUR total` now compiles to `0/+12/+25 EUR`; only Saver is published for a no-paid profile. Unknown prices remain unknown. All 224 unit tests, all 100 browser replays, and build/type/syntax checks pass. Live Kiwi proof remains. |
| 2026-07-31 18:05 CEST | Dissected live trace `chk_ms94pcno92gal3` after the successful Saver click. | The compiler fix worked live: Saver was uniquely selected at 33 EUR. The page then changed to medical cancellation and the verifier recorded the intended outcome and unchanged price, but failed because old fare CTAs remained in a clipped inactive pane. `operationExecutable` accepts their `recoverable` status, so the stale fare group is rebuilt and blocks the visible `0 EUR` decline. This is active-decision lifecycle/availability scoping, not profile policy, slow loading, or a missing control. |
| 2026-07-31 18:20 CEST | Implemented the shared Active Decision Lifecycle across actionability, semantic compilation, browser verification, and backend transition evaluation. Removed stale status override and physical-DOM-removal assumptions. | A control is revealable only through a proven scroll path; non-scrollable hidden/clip panes are inactive. The exact retained-fare replay now records Saver completed and publishes `No thanks` next while keeping old fare nodes in the DOM. All 224 unit tests, all 101 browser replays, and build/type/syntax checks pass. Live Kiwi proof remains. |
| 2026-07-31 18:27 CEST | Dissected live trace `chk_ms95j2a6osa8sa` and replayed its final captured observation through the current readiness classifier. | Live successfully passed fare and medical cancellation, then sampled Kiwi's explicit seat-loading shell and stopped. The current source classifies that same post-navigation shell as `TRANSIENT / POST_NAVIGATION_DESTINATION_NOT_READY`; the live response instead created `surface_ambiguity` and asked the user. Because `npm run dev` launches plain `node apps/web/server.js` with no watcher, backend changes require a restart; extension changes also require reloading the unpacked extension. |
| 2026-07-31 18:39 CEST | Dissected live trace `chk_ms95tuabnfmp1m` through the apparent `Watching page update` stall. | It was not a missed update or readiness latch. The cancellation click changed Kiwi to Seating immediately. `settleTrustedChoiceInteraction()` then called a full `buildPageMap()` on 492 seat controls before marking dispatch (~75 s), and `observePageStateAfterMutation()` compiled the same page again for verification (72,191 ms). Verification succeeded, the loop selected Continue, and clicked it. The remaining bottleneck is unbounded duplicate local perception, not policy, grounding, or seat semantics. |
| 2026-07-31 20:27 CEST | Implemented source-bounded repeated-control perception and simplified post-action choice handling. | Large repeated groups are summarized before section/control graph construction; exact navigation, selected state, profile matches, counts, price range, and expansion metadata survive. Native/pointer custom buttons no longer invoke trusted dropdown cleanup, and dispatch is recorded before the only remaining explicit cleanup path. A 490-seat source replay keeps Continue, omits over 450 repeated cells before compilation, and builds in under the 10-second regression ceiling. All 224 unit tests, all 103 browser replays, and build/type/syntax checks pass. |
| 2026-07-31 20:34 CEST | Dissected live trace `chk_ms99zsndpzu52e` and reverted the entire 20:27 optimization. | Saver actually advanced, but removal of generic choice settlement also removed the commitment evidence required by fare verification. The verifier emitted `EXACT_FREE_OPTION_NOT_VERIFIED`, TaskState reopened the fare, and `Change` sent the agent backward. Restored the prior settlement behavior and unbounded source perception; removed both optimization-specific tests. All 224 unit tests and all 101 browser replays pass. |
| 2026-07-31 20:46 CEST | Dissected live trace `chk_ms9achydm7cig8` after the revert. | The revert worked: no fare loop, and the correct free cancellation choice reached Seating. The perceived hang is real: ~65 s in choice-settlement page reconstruction plus 71.85 s in verification reconstruction. The final handoff is a stale-snapshot consequence, not proof that Continue was absent: the persisted bounded observation contained 11 controls and no Continue even though the live page displayed it moments later. |
| 2026-07-31 20:53 CEST | Mapped the live failure to `FLY_FINAL_ROADMAP.md`. | Immediate work is Roadmap Component #3 at the #13 boundary: create one mutation-versioned, atomic post-action observation shared by verification and planning. After freshness is trustworthy, extend Component #7 plus #10/#14 so a genuinely absent seat exit becomes a profile-resolved discovery requirement rather than a user handoff. |
| 2026-07-31 21:15 CEST | Implemented the Roadmap #3/#13 boundary repair without removing semantic choice proof. Choice settlement now records commitment and closes transient choice UI without compiling the successor page; page state is mutation-versioned and planning rejects stale captures; random-assignment seat inventory is summarized before control-graph construction while exact Continue/Skip controls survive; stage evidence is preserved through compact transport. Removed the speculative 180 ms wait on clean cached observations so readiness polling stays fast. | The new atomic regression proves a late Continue invalidates the first seat snapshot and appears in the bounded retry. Prior fare, immediate-advance, popup-settlement, Kiwi choice, slow-hydration, and payment-review replays remain green. Final verification: 224/224 unit tests, 102/102 browser tests, and `npm run check` pass. |
| 2026-07-31 23:34 CEST | Dissected live trace `chk_ms9g920w5jl1sc` from passenger details through Overview & payment. | Universal decision/profile behavior largely worked live. The remaining seat latency is a perception-order defect: hidden `CVV` evidence classified Seating as payment and disabled the pre-compilation collection bound, causing one 58.5 s planning build and two ~58 s verification builds. The freshness retry correctly caught the popup mutation. Final termination followed Kiwi's own full-page error after the stored `.test` email was filled; Fly then reported the wrong blocker because profile readiness outranked the blocking error surface. |
| 2026-08-01 00:18 CEST | Implemented structure-first collection staging, profile-aware inventory retention, and authoritative typed site-failure ownership. Removed the `step === seats` prerequisite from source bounding and preserved `surfaceClass` through backend normalization. | Live-shaped stale-CVV/random-seat, explicit-seat, late-Continue, and site-error-over-profile regressions pass. Existing complete checkout replays remain green. Final verification: 224/224 unit tests, 104/104 browser tests, and `npm run check` pass. |
| 2026-08-01 12:15 CEST | Dissected live trace `chk_msa7nd4klhvnaa` and compared raw page-state timing with the transported observation. | The stage and profile are correct, and the late collection contract reports 380 omitted random-seat controls. However, raw perception still compiled 392 controls because numeric/split seat nodes evade `looksLikeSeatInventoryElement()`: planning build 35,556 ms, backend policy 5 ms, verification rebuild 35,101 ms. The correct seat popup action then completed quickly, and the new `site_failure` stop worked live. |
| 2026-08-01 12:31 CEST | Implemented structural repeated-action discovery before canonical compilation and removed per-item seat wording as the membership authority. Added a live-shaped split-node fixture with 190 logical seats represented by 380 physical controls. | Random-assignment perception collapses the fixture to fewer than 30 compiled controls in about 0.18 s while preserving Continue; explicit-seat inventory remains available. Final verification: 224/224 unit tests, 105/105 browser tests, and `npm run check` pass. Live Kiwi timing proof remains. |
| 2026-08-01 12:41 CEST | Dissected live trace `chk_msa8k62rb1cjqm` from traveler fields through Kiwi's payment surface. | The structural seat fix is verified live: the full run took about 72 seconds, seat-page observation no longer dominated latency, and the free seat path resolved in seconds. The next core gap is exact enum-option continuity: observation uniquely mapped `SI` to Slovenia, but native execution reinterpreted `si` with substring matching and temporarily selected French Polynesia before the verifier forced exact recovery. Kiwi's final foreground site error was correctly classified and stopped. |
| 2026-08-01 12:54 CEST | Implemented canonical enum-option continuity across the Logical Field Adapter, pipeline contract, candidate/action transport, native/custom execution, and verification. Removed substring selection from native `<select>` actuation and blocked ambiguous canonical mappings before mutation. | A live-shaped ordering with French Polynesia before Slovenia now dispatches exactly one `si` change and verifies Slovenia; duplicate canonical options produce no native action; the existing large custom-country dropdown remains green. Final verification: 224/224 unit tests, 107/107 browser tests, and `npm run check` pass. Live Kiwi proof remains. |
| 2026-08-01 13:06 CEST | Dissected live trace `chk_msa9iz7vqzbffa` and mapped the next gap to `FLY_FINAL_ROADMAP.md`. | Exact nationality binding is now live-proven, and the full profile-directed checkout traversed to Kiwi's final site-error boundary in about 70 seconds. The next order-of-magnitude component is the authoritative transaction-facts/payment-review contract: current invariants lack itinerary segments, normalize an unknown base price to zero, and mix profile decisions into extras. Minor stale-action refusals recovered correctly and are not the primary bottleneck. |
| 2026-08-01 13:34 CEST | Implemented the authoritative transaction-facts ledger and strict payment-review gate across observation, durable state, TaskState, governor inputs, and terminal completion. Removed active parallel price/selection/fingerprint state and the legacy payment-UI-only success path. | Baselines progressively promote only after route, traveler, currency, and total are known; fresh identity wins in `current` so route changes cannot hide behind stale state; only typed commerce decisions enter extras; unknown amounts remain null; final review requires a complete unchanged authorized envelope. Verification: **227/227 unit tests**, **107/107 browser replays**, and `npm run check` pass. Existing traveler, nationality, fare, seat, extras, modal, freshness, and payment-stop successes remain green. |
| 2026-08-01 13:41 CEST | Dissected live trace `chk_msaapqzoz3wuic` against the last successful Seating transition. | The transaction gate was not involved. The medical-cancellation action verified `POLICY_SAFE_CHOICE_ADVANCED`; 422 ms later Fly captured Kiwi's real loading shell at `activeStep=2`, with no seat controls or Continue. `classifyStep()` missed “Select your seats” but matched `baggage/insurance` inside persistent URL query parameters, so readiness incorrectly declared the shell `READY` as extras and TaskState handed off on an empty ambiguity candidate set. The previous successful run happened to capture after 1,006 ms when Continue was already present. Secondary finding: transaction review remains `collecting` because live observations still lack route evidence; this did not cause the seat stop but must be validated before payment review. |
| 2026-08-01 13:48 CEST | Compared successful rerun `chk_msaawr2s9ze6bz` with failed run `chk_msaapqzoz3wuic`. | The rerun observed the full seat map, classified `step=seats`, exposed Continue, opened the seat confirmation, and verified Skip seat selection in about four seconds. It continued through AirHelp and contact entry to `activeStep=4`, where Kiwi itself returned “unexpected weather.” The failed run observed the earlier “Please wait for the seating options to load” shell after 348 ms. This confirms a site-speed-sensitive readiness race; a refresh or faster Kiwi response can mask it, but user behavior is not the architectural cause. |
| 2026-08-01 14:02 CEST | Implemented the shared stage-evidence and destination-readiness contract across extension perception, backend readiness, and action lifecycle. Unified duplicate classifiers, separated route path from visible copy, added generic loading-text evidence, removed decision-group-count readiness, and kept unresolved navigation pending. | The exact Kiwi shell with `holdBags`/`insurance` query parameters now classifies as Seating + `TRANSIENT`; its hydrated successor is immediately `READY`. A full replay initially exposed payment-route/contact-copy precedence, which was corrected before completion. Final verification: **228/228 unit tests**, **108/108 browser replays**, and `npm run check` pass. |
| 2026-08-01 14:10 CEST | Dissected live trace `chk_msabtf22hrljkl` through Overview & payment and compared the visible review with the durable transaction envelope. | Traversal is a strong live success: about 70 seconds end-to-end and roughly five seconds through the free seat path. The next core gap is Roadmap #15 fact compilation: the review visibly proves route/date, Basic Saver, bags, no insurance, random seat, and total, while canonical state still lacks itinerary/fare/seat and misclassifies included baggage. Kiwi's foreground “unexpected weather” error was correctly owned and stopped. |
| 2026-08-01 14:32 CEST | Implemented the Roadmap #15 selected-flight baseline, durable semantic outcome ledger, owned route/review compiler, and strict final reconciliation. Removed compact-recovery outcome replacement and prevented review observations from self-seeding missing baselines. Updated Ali's contact and billing email to `ali@testaztela.com`. | The latest-shaped review compiles city route/date, Basic Saver, no insurance, random seat, both included bags, currency, and total. Stable outcome keys survive rerendered decision IDs; explicit review mismatches block while omitted declined extras retain verified ledger truth. Final verification: **232/232 unit tests**, **109/109 browser replays**, and `npm run check` pass. |
| 2026-08-01 14:41 CEST | Dissected live trace `chk_msacv284ayq3q2` through the real Kiwi payment review. | Traversal and the durable commerce ledger are live-proven: Saver, no insurance, random seat, both included bags, traveler, email, currency, and total survived. The current blockers are evidence ownership, not navigation: loose global route text beat the owned itinerary and parsed phone/SMS help copy; phone did not retain its local value; hidden optional billing name inputs then falsely reopened completed identity. Baseline status is `approved`, contradictions are empty, and `review.ready=false` only reports `review_itinerary_route`. |
| 2026-08-01 14:45 CEST | Corrected Ali's primary phone from obsolete Turkish test data to the user-confirmed Slovenian `+386 70 328 922` and reassessed the trace. | Kiwi's selected `+386` code was correct, so the earlier country-code diagnosis is withdrawn. The prior local-number failure used bad profile data and must be retested. The remaining proven architectural blocker is semantic ownership: loose route text and hidden optional billing controls override owned final-review state. |
| 2026-08-01 15:58 CEST | Dissected corrected-profile trace `chk_msafk45ddm63rq` from traveler entry through Kiwi's real payment review. | The flow repeated successfully in about 96 seconds; nationality, insurance, Saver, free seat, AirHelp decline, email, and transaction outcomes were correct. Seating resolved in roughly six seconds. Correct local phone `70328922` dispatched but did not survive Kiwi's input replacement; canonical alias rebinding failed. Hidden optional billing names then caused the same false profile stop. Baseline remains approved, contradictions remain empty, and false phone/SMS route extraction still leaves only `review_itinerary_route` missing. |
| 2026-08-01 16:02 CEST | Reassessed the repeated final-review failure against the roadmap dependency order. | The next work is not three local fixes. Roadmap #2 and #6 are only partially complete: physical IDs still outrank logical identity during rerender, and fresh raw DOM can recreate inactive requirements or facts instead of reconciling through owned durable state. Phone, hidden billing names, and false route extraction are the current proof. Finish this central invariant before further site-specific or downstream tuning. |
| 2026-08-01 16:18 CEST | Implemented the shared semantic identity/lifecycle reconciliation repair across extension execution, backend aliases, profile readiness, scheduling, and transaction perception. | Rerender recovery now relocates by stable logical identity; hidden optional billing controls cannot reopen a completed profile; only owned itinerary structures can produce city routes and phone/SMS help is excluded. No Kiwi selector, timeout, or extra retry loop was added. Verification: **233/233 unit tests**, **110/110 browser replays**, and `npm run check` pass. Live Kiwi confirmation remains. |
| 2026-08-01 16:31 CEST | Dissected live trace `chk_msago9keplhwfk` through the real Kiwi payment-review page and the subsequent overrun. | Traversal is now a major live success, including correct contact phone. The root remaining failure is terminal authority: strong payment-review evidence did not latch because `page.step` stayed `confirmation`, so lower-level candidate/profile logic continued into payment and billing. The trace also proved two supporting contract gaps: mutable values change field identity, and generic two-word phrases can still become route facts. |
| 2026-08-01 16:49 CEST | Implemented authoritative payment-review terminal ownership, contact-safe boundary handling, immutable field identity, and strict route endpoint ownership. | Final review is recognized from mutually reinforcing owned evidence rather than one stage label. Email/phone may finish, then verified review latches; incomplete review freezes safely. Payment, billing, legal, Edit, and generic work cannot follow. `DIRECT → FLIGHT` is rejected and controlled input rerenders preserve identity. Verification: **237/237 unit**, **112/112 browser**, and `npm run check` pass. Live Kiwi acceptance remains. |
| 2026-08-01 17:10 CEST | Dissected live acceptance trace `chk_msai899mz1wfey` through final contact and the payment-review boundary. | Terminal dominance is live-proven: after correct email and Slovenian phone, Fly made no payment-method, billing, legal, card, or Pay mutation. It froze safely rather than claiming success. Positive completion is still blocked by transaction-fact provenance: the visible Antalya → Istanbul route was not compiled, while early cancellation marketing contaminated the baseline fare and created a false `FARE_BRAND_CHANGED` contradiction. |
| 2026-08-01 17:34 CEST | Implemented the authoritative transaction-evidence compiler and derived process-awareness contract. | Smallest owned itinerary containers now produce route facts; every material fact retains provenance; approved baselines fill unknowns without review self-seeding; TaskState reports current position, achievements, unresolved work, and final outcome. Exact real-review-card, marketing contamination, late-fare, and terminal process regressions pass. Live Kiwi proof remains. |
| 2026-08-01 17:42 CEST | Tightened fare identity before final validation. | Official fare identity now requires an owned summary row/line and normalizes the separately verified selection ledger. Token-subset equivalence was removed, so `Economy` cannot silently equal `Basic Economy`. Verification: **240/240 unit**, **114/114 browser**, and `npm run check` pass. |
| 2026-08-01 20:06 CEST | Analyzed live trace `chk_msajk04t0f70eb` after the transaction-evidence implementation. | Traversal, profile entry, final-review extraction, and the no-payment safety boundary all passed live. Certification still stopped correctly because no pre-review route anchored the baseline. The remaining root gap is one component boundary: recognize the persistent route header before final review and preserve typed transaction evidence through server sanitization. |
| 2026-08-01 20:20 CEST | Implemented universal pre-review transaction anchoring and lossless transaction-evidence transport. | Exact route-shaped checkout owners now establish identity even as generated `div`/`span` UI; the server uses one complete fact compactor; evidence retains segment identity through normalization and reconciliation; new typed observations cannot fall back to unproved values. Final-review self-seeding and all payment boundaries remain unchanged. Verification: **241/241 unit**, **116/116 browser**, and `npm run check` pass. Live acceptance remains. |
| 2026-08-01 20:33 CEST | Analyzed live acceptance trace `chk_msap7ph8zcet8r` through terminal certification. | The run completed traveler data, user-profile decisions, Saver, random seat, AirHelp decline, and contact in about 102 seconds. Final facts matched `Antalya → Istanbul`, Basic Saver, Ali SIFRAR, 1,637.80 TRY, included bags, no insurance, and random seat; `payment_review_reached` suppressed further planning. No irreversible action was proposed. The next precision replay is rejecting passenger-age text that was also retained as route-shaped baseline noise. |
| 2026-08-01 20:38 CEST | Implemented universal route-endpoint qualification and canonical segment coalescing from the live Kiwi evidence noise. | Demographic/age/measurement/numeric ranges cannot become route endpoints; duplicate observations merge without losing the richer date or evidence identity. The live-shaped replay retains only `Antalya → Istanbul · Tue 9 Feb`. Verification: **242/242 unit**, **116/116 browser**, and `npm run check` pass. The next active milestone is current-code GoToGate acceptance. |
| 2026-08-01 20:43 CEST | Updated Ali's authoritative traveler profile email. | Contact email and billing email are now both `ali@aztela.com`; phone and all booking preferences are unchanged. Historical test fixtures and trace evidence remain untouched. |
| 2026-08-01 20:52 CEST | Dissected two current-code GoToGate traces after the first cross-airline acceptance run. | Profile filling is correct and reproducible. The first blocker is broad-owner requiredness promoting an optional negated newsletter control into a mandatory marketing decision; the next latent defect is paid bundle cards compiled as free and detached from their no-bundle actuator. The repair belongs to the universal decision/effect compiler, not a GoToGate workflow. |
| 2026-08-01 23:11 CEST | Dissected Kiwi trace `chk_msav1t7zv4s50j` against accepted trace `chk_msap7ph8zcet8r` and the uncommitted route diff. | The free cancellation choice succeeded and navigation reached Seating. The new run captured a blank destination shell after ~0.4 s and handed off because readiness lost the separately transported `lastAction` context; the accepted run waited long enough to see Continue. Route changes are isolated from readiness, but the trace also disproves complete route hardening by accepting marketing prose as a route. |
| 2026-08-01 23:29 CEST | Implemented the shared destination-readiness, local decision/effect, and positive route-ownership contracts. | Durable session action/lifecycle evidence now keeps blank destinations transient; exact owner requiredness leaves negative marketing optional; discounted/current prices outrank benefit copy while genuine Included fares remain free; paid bundle options merge with their exact decline; bounded routes require positive itinerary qualification through browser and server transport. Verification: **244/244 unit tests**, **118/118 browser replays**, and `npm run check` pass. |
| 2026-08-01 23:53 CEST | Dissected successful Kiwi trace `chk_msaw7b789a79pn` and looping GoToGate trace `chk_msawblzqv9kinr`. | Kiwi remains green. GoToGate accepted the correct Flexible Ticket decline, but Fly split the dropdown choice and confirmation popover into separate decisions, failed to propagate child completion to the parent, and repeatedly made the selected option actionable. The next core repair is universal parent/child decision-episode reconciliation plus semantic cycle detection. The live bundle wording also exposed a separate current-price parsing gap that requires an exact replay. |
| 2026-08-02 CEST | Implemented universal multi-surface decision episodes and the live-shaped bundle effect repair. | Parent/child outcome metadata now survives candidate, action, browser, and backend verification; completed selected controls are suppressed; only the exact unique expanded opener can dismiss a completed choice surface; written-currency accessibility evidence compiles compact current prices correctly. Verification: **246/246 unit tests**, **119/119 browser replays**, and `npm run check` pass. Next: fresh live GoToGate acceptance to verified payment review. |
| 2026-08-02 00:39 CEST | Dissected live GoToGate trace `chk_msayckcjf2yoni`. | The parent/child decision episode works live and the former loop is resolved. The next exact action was the unique expanded dropdown opener, but candidate construction authorized it by locally cloning surface ownership while the governor reclassified the original page-owned control and denied it. Next repair: one canonical parent-surface exit capability shared by builder, governor, dispatch, and verifier; delete the local clone and duplicate browser inference. |
| 2026-08-02 01:05 CEST | Implemented canonical parent-surface exit capability continuity. | One fresh typed ownership contract now survives TaskState, candidate construction, governance, browser dispatch, and verification. The local candidate ownership clone was removed; browser and governor consume the shared validator; exactly one expanded opener is required. Stale proof and multiple openers fail closed. Verification: **247/247 unit tests**, **119/119 browser replays**, and `npm run check` pass. Fresh live GoToGate acceptance remains. |
| 2026-08-02 CEST | Implemented canonical logical-component commitment from the repeated GoToGate phone-prefix failure. | Choice-capable profile fields no longer complete from displayed text alone while an owned option surface is active. Exact option binding, popup/focus settlement, canonical parent verification, and narrow stale-presence-validation retirement now share one outcome across planner, executor, and verifier. Direct/native choice and Kiwi checkout replays are preserved. Verification: **249/249 unit tests**, **120/120 browser replays**, and `npm run check` pass; live GoToGate acceptance remains. |
| 2026-08-02 11:50 CEST | Dissected live GoToGate trace `chk_msbm3wcj7238iv` after the logical-component repair. | The repair passed live. Continue opened Seat selection successfully, but the foreground popover expanded 245 repeated controls before structural bounding and blocked synchronous observation for 97.6 seconds. Verification took 67 ms and succeeded; lifecycle reporting then stopped. The next core repair is one shared high-cardinality collection boundary for page and foreground surfaces, not a timeout or site-specific seat click. |
| 2026-08-02 CEST | Implemented the shared high-cardinality foreground collection compiler. | Page and foreground observation now consume the same structural seat inventory. Random-assignment profiles retain exact exits and collection evidence without compiling every seat; explicit seat profiles still retain the full inventory. Visual/fingerprint evidence is bounded and per-option Headless UI nodes no longer become overlay candidates. Verification: **249/249 unit tests**, **121/121 browser replays**, and `npm run check` pass. Fresh live GoToGate acceptance remains. |
| 2026-08-02 12:11 CEST | Dissected GoToGate trace `chk_msbn2x2vs9sb1g` after the foreground collection repair. | The performance repair passed live: the material seat observation built in 1,985 ms, the fresh retry in 2,034 ms, outcome verification in 2 ms, and lifecycle reporting resumed. The stop was `UNAPPROVED_SELECTED_EXTRA`: a provisional selected background `seatmap_true / Add to cart` control became a paid seat outcome although the modal said `Not selected`. The modal's price-key disclosure also became a paid option and generic `standard` wording misclassified the seat surface as a fare package. Next repair: one parent/child commitment episode that separates intent, information, actual selection, and committed transaction effect. |
| 2026-08-02 CEST | Implemented authoritative deferred-choice commitment from the live GoToGate seat trace. | Exact seat context now outranks generic fare adjectives; ARIA-owned price legends remain information; explicit Skip/No-thanks commands join the foreground decision; a paid-looking parent opener remains `option_pending` while its owned child says `Not selected`; pending intent is excluded from transaction facts. The governor remains strict. Verification: **249/249 unit tests**, **122/122 browser replays**, including the exact selected-parent → child-modal → Skip regression. Fresh live GoToGate acceptance remains. |
| 2026-08-02 CEST | Dissected GoToGate trace `chk_msbnzmvhdlkm2t` through the final two-leg no-seat confirmation. | All earlier GoToGate blockers remained fixed and Fly grounded the correct final `Continue`. The new surface wording caused the background seat opener to lose `option_pending` state and enter transaction facts as a paid seat, so the governor correctly blocked. The unfinished core contract is a durable, decision-scoped episode that carries commitment across leg and confirmation surfaces; another phrase exception would be patchwork. |
| 2026-08-02 CEST | Implemented the authoritative multi-surface decision lifecycle and removed duplicate commitment inference. | Commerce episodes now rotate by semantic family/subject, retain both seat-segment outcomes, enter `confirmation_pending`, and commit `random_assignment` only after verified final confirmation. Transaction facts consume terminal episode outcomes; provisional background openers cannot become purchases. The exact two-leg replay and genuine paid-seat branch pass. Full verification: **251/251 unit tests**, **123/123 browser replays**, and `npm run check`. Fresh live GoToGate payment-review acceptance is next. |
| 2026-08-02 13:08 CEST | Dissected live GoToGate trace `chk_msbp299gi8ivym` after the decision-lifecycle repair. | The repair passed live and GoToGate reached its real payment form. Fly then waited 24 times for 20.3 seconds because browser classification preferred incidental confirmation wording and backend readiness could not see intentionally non-executable card-field evidence. Terminal review was not certified; route/fare/outcome reconciliation also remains incomplete. The next core repair is one authoritative, non-action terminal evidence contract shared across perception, readiness, TaskState, and transaction review. |
| 2026-08-02 17:59 CEST | Dissected live GoToGate trace `chk_msbzh3c9doxhp1` after canonical decision-owner and verified-journal changes. | Checkout reached verified payment review safely in about 112 seconds with no loop or irreversible action. The new journal failed live: verified decisions carried nested episode lineage, but reducer continuation/admission lost or collided it; all six later extras shared one owner, Flexible Ticket split parent/child owners, and the final journal/ledger were empty. Next repair is one atomic verified-action outcome committer plus journal-coverage reconciliation, not more site wording rules. |
| 2026-08-02 19:16 CEST | Implemented the authoritative verified-action outcome committer and journal-coverage gate. | Exact decision instance metadata now survives action transport; one lineage reader owns continuation and verification; exact sibling group identity prevents shared-label collisions; parent/child and multi-leg seat outcomes upsert once with segment evidence; same-turn transaction reconciliation consumes a changed journal; terminal readiness fails closed on missing coverage. Validation: 257/257 unit tests, full checks, all modified browser replays green, and both unrelated full-run timeout cases green in isolation. Fresh Kiwi/GoToGate canaries remain. |
| 2026-08-04 14:41 CEST | Dissected Turkish trace `chk_msen80j7gvbmu1` after the progressive terminal/filter implementation. | The new reverse-ARIA membership was present live: search control `ctrl_select_h1x3k8fb` belonged to dropdown `atw-el-302` and exposed a visible, enabled, hit-tested type actuator. It was nevertheless removed because a discarded broad opener control and the child filter produced `unresolved_equal_priority` graph conflicts. Fly fell back to three reveal/reopen attempts, eventually selected Slovenia, restored phone, selected title, and clicked Continue. Turkish then opened “We are currently unable to process your request”; Fly stopped safely. No payment evidence was reached, so the progressive terminal repair remains replay-only. |
| 2026-08-04 16:34 CEST | Implemented actuator-scoped cooperative widget ownership and site-failure transition dominance. | A strictly narrower atomic control now resolves a broad composite overclaim only when its state, activation, and every supported operation share one exact actuator. True same-actuator competitors remain unresolved and blocked. A `site_failure` foreground can produce observed mechanical change but cannot complete `checkout_stage_advanced`. The Turkish-shaped filter pipeline, genuine-conflict negative, and site-failure transition regression pass. Verification: **272/272 unit tests, 131/131 browser replays, and `npm run check`**. Fresh Turkish, Kiwi, and GoToGate live proof remains. |
| 2026-08-04 16:42 CEST | Dissected Turkish traces `chk_mserk8gq63v9d5` and `chk_msernlo5ned5cj`. | Cooperative ownership worked live: Fly selected the visible owned search control instead of scrolling. The adaptive candidate copied canonical `+386` into the filter and verified only that the query text changed; the widget required a mechanically suitable query such as `386` or `Slovenia`, so the exact option did not appear until manual correction. Profile scheduling filled the local number before its prefix and title later because independent discovery order outranked dependency/document order; selecting the prefix rerendered and cleared the number, forcing a refill. The final AI Continue used `native_click`; Turkish immediately published an explicit site-failure modal, while the user's subsequent browser interaction restarted successfully. The resumed session physically reached `/booking/payments/`, and progressive terminal evidence recognized the boundary, route segments, and `317.54 EUR`; certification correctly remained false because the mid-checkout restart baseline was `66.25 EUR`, lacked the itinerary, and had no earlier outcome journal. No payment/card/legal/purchase action executed. |
| 2026-08-04 CEST | Implemented semantic-target/mechanical-query separation, dependency-aware profile scheduling, durable exact-option verification, and browser-faithful stage advancement. | The adaptive episode tries bounded codec/option hypotheses without treating filter text as profile completion; temporary portalled filters cannot become saved-profile fields; exact selection verifies `phone_country_code=+386` after surface dismissal. Independent fields follow visual order, composite dates retain canonical day/month/year order, and phone prefix precedes local number. Exact stage navigation now uses governed browser pointer then one native fallback; the redundant synthetic pointer strategy was removed. Verification: **272/272 unit tests, 133/133 browser replays, and `npm run check`**. Fresh Turkish live certification and accepted canary reruns remain. |
| 2026-08-05 CEST | Completed EasyJet composite state/actuator settlement from trace `chk_msg0xgv31ltvep`. | The earlier discovery repair did run: it opened the title widget and clicked `Mr`. The stop persisted because the verifier read the blank actuator representation instead of the exact state sibling, then substring matching admitted `Mrs` for `mr`. One merged logical component now retains both mechanics and state evidence; bounded exact choice matching prevents prefix collisions. Verification: **275/275 unit tests, 136/136 browser replays, and full checks**. Fresh live EasyJet acceptance remains. |
| 2026-08-05 CEST | Dissected EasyJet trace `chk_msg26ymqhrvb9z` after composite settlement. | The repair passed live: exact `Mr` committed. Fly then promoted four hidden zero-size emails from dormant branches and a synthetic hidden confirm-email error into current requirements, while treating the visible age placeholder as selected. It stopped safely without an unauthorized action. Next repair is universal active-lifecycle requirement admission and shared placeholder commitment, not an EasyJet workflow. |
| 2026-08-05 CEST | Implemented authoritative active-requirement admission and shared placeholder commitment. | Canonical controls now retain a representation lifecycle independent of semantic recognition. Dormant hidden forms cannot create profile goals, skill atoms, inferred blockers, or validation; hidden state remains usable when an exact rendered widget owns it. Removed duplicate confirm-email inference and unified placeholder handling. The EasyJet-shaped hidden-branch → hydrated-contact replay passes, as do **275/275 unit tests** and **137/137 browser replays**. Fresh EasyJet and accepted-site live canaries remain. |
| 2026-08-05 CEST | Implemented authoritative Selected Booking acquisition and single-source derived facts. | Flight-selection `transaction-facts/v2` is captured before navigation, validated at `/agent/session`, and persisted through the existing transaction invariant baseline. Age ignores mutable/current-page dates, missing departure evidence outranks semantic ambiguity, and generic containers cannot trigger a model call from disabled navigation alone. **285/285 unit tests, 138/138 browser replays, and repository checks pass.** Fresh EasyJet plus Kiwi/GoToGate live acceptance remains. |
| 2026-08-05 CEST | Dissected fresh EasyJet trace `chk_msg7m2u5rjzrga` after Selected Booking persistence shipped. | Persistence and precise recovery behaved correctly, but acquisition did not: both route/date legs were visible in exact itinerary Edit controls while `transactionFacts.itinerary.segments=[]`. The session therefore had no selected-booking baseline and could not derive age. After handoff, passive capture also rescanned extension-driven mutations repeatedly. Next repair is universal owned-itinerary evidence admission plus event-driven capture, not an EasyJet workflow or relaxed safety gate. |
| 2026-08-05 CEST | Implemented exact itinerary-action evidence, bounded capture lifecycle, and visible acquisition diagnostics. | `Edit Ljubljana to Edinburgh flight on 15th August 2026` and the return now compile to two authoritative ISO-dated segments; unrelated Edit text is rejected; the controls are context-only and cannot enter planning. Capture ignores extension UI mutations and stops once a session exists. The sidebar always reports selected booking captured/missing plus route, dates, and source. **285/285 unit tests, 138/138 browser replays, and repository checks pass.** |
| 2026-08-05 CEST | Repaired the live EasyJet field-contract continuity failure from `chk_msg80sof1olh25`. | The trace proved itinerary acquisition and deterministic age derivation were already correct: TaskState published `age_at_departure=23`. Candidate construction then independently rebuilt the field without the durable baseline and returned zero actions. Current profile-goal execution now treats the published task as semantic authority and rebinds only fresh mechanics. The exact replay no longer injects Selected Booking into candidate construction. **285/285 unit tests, 138/138 browser replays, and repository checks pass.** |
| 2026-08-05 CEST | Repaired the deeper EasyJet parent→child field-ownership conflict from `chk_msgcbwd0wx62ab`. | The published age task survived actuator binding and opened the correct control, but a transient page-readiness result prevented task transfer to the visible options; generic foreground planning then mislabeled `18+` as paid and raw-value verification could not relate it to age `23`. Verified openers now preserve exact task ownership, and one shared semantic compatibility contract drives candidate admission and verification. **285/285 unit tests, all 139 browser cases green, and repository checks pass.** Fresh live acceptance remains. |
| 2026-08-06 CEST | Completed authority convergence after EasyJet `chk_msha4d22ller5w`. | Root cause combined extension self-occlusion, duplicate Continue representations, split profile-completion authorities, and duplicated recovery state. Fly-owned UI is now transparent only around exact site hit-testing/dispatch and has no focused-tab/rAF dependency; stage exit is candidate-only; exact child settlement upgrades one canonical verifier; TaskState persists only canonical verification; recovery is one current-obligation episode; legacy live requirement inference and two dead classifier/extractor modules are removed. Delayed choice readiness now recompiles fresh candidates and useful progress cannot restore stale failed methods. **286/286 unit tests, 142/142 browser replays, the focused background-tab isolation replay, and `npm run check` pass.** Fresh EasyJet, then Kiwi/GoToGate live canaries remain. |

## Update rule

Update this file after every material live trace, root-cause decision, implementation, or verification run. Update `FLY_COVERAGE_MATRIX.md` in the same change, and update `FLY_FINAL_ROADMAP.md` whenever component status or priority changes. Keep **Current focus — read this first** short and authoritative; move detailed evidence into the live table and progress log.
